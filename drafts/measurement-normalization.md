# Measurement & Normalization Contract — черновик (informative)

**Статус:** DRAFT. Не является частью нормативного контракта. Это размышление, а не спецификация. Все девять вопросов слоя измерения (**A–I**) закрыты (§7), и слой измерения канонизирован в `DOF-SPEC.md` **`v0.4`** (§11.3). Правка по **I** разобрана в §11.1, что осталось — §11.2.

**Происхождение:** выросло из разбора `drafts/real-idea-1.md` и `drafts/real-idea-2.md` и из расхождения между нормативной математикой и `references/dof-assessment-toolkit.md`.

## 0. Зачем этот документ

Нормативная математика (§4.1 `DOF-SPEC`) определена на `current_dof ∈ [0,1]` — иначе `ln(DoF)` и штраф `−∞` не имеют смысла. Но нигде в стандарте не сказано, **откуда берётся это число**. Единственный документ об измерении — `references/dof-assessment-toolkit.md` — даёт три линзы с несовместимыми единицами:

| Линза | Сырая метрика | Тип величины | Что означает 0 |
|---|---|---|---|
| Variety (Ashby) | число независимых векторов отклика | неограниченное целое | нет откликов (хрупкость) |
| Options (Real Options) | **обратная** стоимости перехода | неограниченное положительное | переход бесконечно дорог (заперто) |
| Constraint | Total − ActiveConstraints | целое, может быть ≤ 0 | все переменные заморожены |

«Integration Note» просто сообщает, что эти сырые значения «кормят `Σ ln(DoF)`». В результате **главный интерфейс стандарта недоопределён**: два корректных анализатора, применив разные линзы к одному и тому же миру, получат несравнимые числа. §7 требует бит-эквивалентности математики *при данных входах* — но сами входы не нормированы.

Цель этого черновика — предложить явную нормирующую функцию `ψ` для каждой линзы и решить, как линзы соотносятся между собой. Это **слой измерения**: он должен стоять перед `current_dof`, но не подменять ни математику, ни агрегацию.

## 1. Требования к нормировке

Пусть для каждой линзы `ψ: raw → [0,1]`.

- **R1. Коллапс = 0.** `ψ(collapse) = 0`. Это единственная точка, где математика даёт `−∞`. Линзы, у которых «0 сырого» не означает «нет возможностей» (Options: 0 стоимости — это, наоборот, полная свобода), должны маппиться так, чтобы **запирание** попадало в 0.
- **R2. Монотонность.** Больше возможностей ⇒ `ψ` не меньше. Нормировка не должна создавать нелокальные артефакты, иначе сравнение вариантов теряет смысл.
- **R3. Насыщение.** `ψ` ограничена сверху: неограниченный сырой вход → 1. Нужен явный референс, иначе «неограниченный рост» просто переезжает внутрь.
- **R4. Декларируемость.** `ψ` — часть входа, а не скрытая эвристика. Реализация обязана объявить свою `ψ`; тогда §7 остаётся выполнимым (детерминизм при зафиксированных входах).
- **R5. Совместимость с `dof_known`.** `ψ` не применяется к «неизвестному». Если DoF не измерен, ставится `dof_known = false`, и нормировка **не фабрикует** значение (Axiom 5, Axiom 7).
- **R6. «Нет возможностей» ≠ «недостижимо».** Это разные случаи; см. §4.
- **R7. Заморозка шкалы (единая линейка).** Масштабные параметры нормировки (`V_env`, `c₀`, `T`, граф `G`) фиксируются состоянием `S` и применяются **одинаково** ко всем сравниваемым вариантам. Меняется только измеряемая величина, не масштаб. Иначе вариант может улучшить оценку подгонкой знаменателя, и сравнение (монотонность по `V`, R2) теряет смысл. Побочно это делает нормировку «частью входа», что связывает с `psi_id` (вопрос F).

### 1.1 Где живёт декларация: `psi_id` и дайджест (вопрос F — закрыт)

R4 требует, чтобы `ψ` была частью **входа**, а R7 — чтобы шкала была **заморожена на `S`**. Отсюда форма хранения:

| Уровень | Что хранится |
|---|---|
| `SystemStateMatrix` | `psi = { id, digest }` — имя и версия процедуры измерения плюс хеш полного текста декларации |
| отчёт §6 | полный текст декларации, её `psi_id`, `digest` и значения всех нормирующих величин, реально использованных в расчёте |
| вне модели | внешний реестр деклараций — **необязателен**: текст едет в отчёте, поэтому автономная реализация остаётся конформной |

**Что входит в декларацию** (закрытый перечень; всё это вход, а не вывод):

- набор линз и их канонический порядок (по умолчанию три: `ψ_var`, `ψ_opt`, `ψ_con`);
- Variety: `V`, `V_env` и определение счётчика откликов;
- Options: бюджеты `C_g`, разбиение на блоки, наблюдаемые курсы обмена, политика «максимум на один переход»;
- Constraint: `F`, `F_env` и определение множества различимых переменных (§2.3);
- приор для `u₀` — форма, параметры, обоснование (§8.7);
- `T_rec(X)`, класс средств `M(S)` и вердикты достижимости (§8.9, §9.3);
- источники длительностей `t_m`, `t_v`, `t_a±`, `d(o)` и имя Perception-процедуры (§8.6);
- `τ` и все масштабы, замороженные на `S`.

**Разложение по уровням.** На **состояние** (одинаково для всех вариантов одного цикла): набор линз и их порядок, средства агента по ресурсам с единицами, выведенные группы, наблюдаемые курсы и объявленный мандат, `τ`, приор `u₀`, класс `M(S)`, версия процедуры. На **сущность**: `V` и `V_env`, `F` и `F_env`, `T_rec`, длительности.

**Требование канонизации.** Дайджест сравним только при канонической сериализации декларации: фиксированный порядок ключей и полей, без свободных формулировок. Иначе одинаковый по смыслу вход даст разные хеши, и проверка «одна линейка» будет срабатывать ложно. Механизм при этом не изобретается: §10 `DOF-SPEC` уже применяет публикацию SHA-256 к файлу спецификации.

**Что это даёт.** R7 становится **проверяемым**, а не обещанным: вариант не может улучшить оценку подгонкой знаменателя, потому что `S` и `S'` обязаны объявить один и тот же дайджест, и расхождение обнаруживается, а не растворяется в числах. §7 (бит-эквивалентность при данных входах) становится **сверяемым**: хеши совпали — термы обязаны совпасть; хеши разошлись — сравнение недействительно. Плюс воспроизводимость: `psi_id` вместе с версией говорит, какой линейкой получено конкретное решение, поэтому исправление нормировки не переписывает задним числом смысл старых отчётов.

## 2. Три линзы → три ψ

### 2.1 Variety lens (Ashby) — разнообразие откликов

Сырое: `V` — число уникальных независимых векторов отклика. Проблема — неограниченность. Нормировка идёт по среде: принцип Ashby требует «разнообразие системы ≥ разнообразие среды». Итоговая формула (**решено**, вывод — §10):

```
ψ_var = 0,                 если V = 0
ψ_var = V / (V + V_env),   иначе
```

`V_env` — характеристика **внешней среды** (внешние возмущения, см. §10.2). Нет откликов ⇒ `0` (коллапс, R1); тихая среда (`V_env = 0`) при `V > 0` ⇒ `1`. Форма — «доля», а не порог (§10.3).

### 2.2 Options lens (Real Options) — стоимость перехода

Сырое: `c` — стоимость перехода из `S`; большая стоимость ⇒ мало DoF. «Обратная стоимость» неограничена и не нормирована. Предлагается нормировка через референсный масштаб стоимости `c_0`:

```
ψ_opt(c) = 1 / (1 + c / c_0)          (гиперболический профиль)
```

Свойства: `ψ(0) = 1` (свободный переход), `ψ → 0` при `c → ∞` (запирание), монотонно убывает. Альтернативный профиль с тем же смыслом, но круче у нуля: `ψ_opt = exp(−c / c_0)`. Для случаев, где важны порядки стоимости, возможен вариант `ψ = max(0, 1 − log(c)/log(c_max))`.

**Решено (§8.8):** гипербола **отклонена**, логарифм отвергнут (смешивает «дорого» и «невозможно»), принята **экспонента** — см. §8.8. Профиль не скалярный, а разложенный по блокам ресурсов:

```
ψ_opt = Π_g f_g(x_g),      f_g(x) = 4^(−x),      x_g = c_g / C_g
```

### 2.3 Constraint lens — свободные переменные

Сырое: `F = Total − ActiveConstraints`. Смысловая нагрузка линзы — **топологическая свобода** (размерность доступного пространства состояний).

**Решено:** Для устранения проблемы «скрытого знаменателя» (зависимость `Total` от мнения анализатора) принята нормализация по принципу «доли свободы» относительно внешнего зажима:

```
ψ_con = F / (F + F_env)
```

где `F` — число свободных переменных, а `F_env` — количество переменных, заблокированных внешней средой (перцептивный зажим). 

**Смысл:** Это превращает Constraint-линзу в **бюджет дистанции до свободы**. Если сущность находится в «коридоре» (ограниченном пространстве), движение по нему может не менять текущее значение `ψ_con`, но оно сокращает расстояние до выхода, где `F_env` станет равным 0. Это позволяет `projected_dof_delta` отражать движение к топологической свободе, даже если текущий зажим всё ещё велик.

### 2.4 Единое правило вырожденного случая

Guard `(0,0)` перестаёт быть частностью Variety и становится **общим правилом всех трёх линз**: если «числитель» и «внешний зажим» нулевые одновременно, линза даёт `0`.

| Линза | Вырожденный случай | Значение |
|---|---|---|
| Variety | `V = 0` (в том числе при `V_env = 0`) | `0` |
| Options | нет ни одного достижимого перехода | `0` |
| Constraint | `F = 0` и `F_env = 0` | `0` |

Почему правило общее, а не своё у каждой линзы:

- **Тотальность.** `F/(F + F_env)` при `F = 0, F_env = 0` даёт `0/0`, то есть `NaN`. `NaN` в сумме отравляет индекс целиком: сравнение вариантов становится бессмысленным из-за одного пассивного объекта в тихой среде. Это не неприятность реализации, а отсутствие определённости там, где §7 требует совпадения чисел.
- **Никакого выбора конвенции.** Без явного правила одна реализация вернёт `0` (коллапс), другая `1` («свобода»), третья `NaN → 0`. Три разных индекса на одном входе, и подделывающий выбирает удобную себе. Единое правило убирает сам выбор.
- **Смыкание с §4.2.** `ψ_l = 0` — не приговор, а передача решения дальше: `lens-zero → entity-zero → §4.2` (исключение известного ноля без пути восстановления). Так пассивный объект не получает бесконечного штрафа и не исчезает молча. Для Options это уже записано (§8.8, B-x: «набор переходов пуст ⇒ `ψ_opt = 0` → судьбу решает §4.2»); теперь правило одно для всех.

## 3. Как соотносятся линзы

Три линзы мерят разное: Variety — устойчивость, Options — гибкость/стоимость перехода, Constraint — топологическую свободу. Складывать их нельзя по той же причине, по которой нельзя компенсировать уничтожение одного пространства возможностей ростом другого (черновик `real-idea-1` §9–10): сумма и арифметическое среднее позволяют одной линзе **маскировать** провал другой.

Варианты:

| Вариант | Формула | Свойство |
|---|---|---|
| 1. Альтернативы | для решения выбирается релевантная линза, `ψ` применяется к ней | просто, декларируемо, без взвешивания |
| 2. Минимум | `ψ = min(ψ_var, ψ_opt, ψ_con)` | консервативно: DoF ограничен узким местом |
| 3. Произведение | `ψ = ψ_var · ψ_opt · ψ_con` | 0 в любой линзе ⇒ общий 0 (коллапс по любому каналу) |

**Решено (вопрос D закрыт): Вариант 3 — произведение, с разбивкой в отчёте.**

```
ln(ψ_var · ψ_opt · ψ_con) = ln ψ_var + ln ψ_opt + ln ψ_con
```

**Одно число на сущность, три терма в отчёте.** Исчисление §4 (исключение §4.2, `net_delta`, выбор §4.5) работает с `current_dof` — одним числом, и тип `float` в `[0,1]` не меняется: правка аддитивная. При этом отчёт §6 обязан показывать вклад **каждой** линзы отдельно, её собственный `dof_known` и **связывающую** линзу — тем же приёмом, что «связывающий блок» в §8.8. Так отчёт говорит не только «сколько», но и «почему»: «узел заперт не деньгами, а разнообразием».

**Почему не минимум.**

- **Он убивает градиент.** `0.9 · 0.1` и `0.95 · 0.1` дают один и тот же минимум `0.1`: улучшение не-связывающей линзы не видно в `ΔIndex`, и выбор уходит в лексикографический tie-break. При произведении: `0.09 → 0.0955`, `ΔIndex = +0.054` ната — вариант виден. То есть произведение даёт награду за улучшение **каждого** канала, минимум — только за узкое место.
- **С неизвестным множителем он не определён** (§8.2), а произведение даёт `u` на терме.
- **Аргумент «минимум консервативнее» не работает.** Для значений из `(0,1]` произведение строго не мягче минимума (`0.9·0.1 = 0.09 < 0.11`), поэтому минимум не покупает ничего взамен — ни аддитивности в натах, ни разложения.

**Набор линз заморожен.** Три линзы по умолчанию; четвёртая — лишний множитель, то есть сдвиг абсолютных значений всех сущностей и другой максимум штрафа за незнание (§8.2). Это **нормирующий факт**, как число блоков в §8.8: добавление линзы — версионное событие, а канонический порядок линз входит в декларацию (§1.1).

## 4. Достижимость: «нет возможностей» против «возможности недостижимы»

Из `real-idea-2`: «недостижимая возможность не является возможностью в смысле DoF». Формализация:

Пусть для контекста `C` (набор ограничений) определена функция достижимости `reach_C(state, possibility) ∈ {0, 1}`.

- Возможность **проверяемо недостижима** и это **известно** → она не учитывается в сырых `V`, `c`, `F`.
- Достижимость **неизвестна** → `dof_known = false`; значение не фабрикуется (R5).

Разграничение критично. Без него reachability-фильтр превращается в легальный способ обнулить неудобную сущность — прямое нарушение Axiom 5 и Axiom 7.

## 5. Согласование с текущим стандартом

- Значения из `ψ` попадают в `current_dof ∈ [0,1]`, как требует §3.1.
- `ψ = 0` при `dof_known = true` — это ровно коллапс (§4.1, `−∞`).
- `projected_dof_delta` — это разность `ψ` прогнозируемого состояния, а не разность сырых величин. То есть `ψ` применяется **до** вычитания, не после.
- `dof_known` ортогонален `ψ`: он про наличие измерения, а не про величину.
- Опция, которая не успевает до коллапса (`estimated_duration_mks > τ`), изымается из набора **до** сравнения — она не штрафуется, а не существует (§8.6).

## 6. Проверка на контрпримерах

**Пример 1. «Необратимый процесс: DoF состояний ↑, DoF действий ↓».**
Две линзы: `ψ_var` (состояния) и `ψ_opt` (действия). До: `ψ_var = 0.4`, `ψ_opt = 0.8`. После запуска процесса разнообразие состояний растёт (`ψ_var → 0.9`), но стоимость остановки уходит в бесконечность (`ψ_opt → 0.02`).
Сумма: `1.2 → 0.92` (падение почти незаметно).
Произведение: `0.32 → 0.018` (обвал, потеря почти на порядок).
Минимум: `0.4 → 0.02`.
Вывод: сумма размывает опасность, произведение и минимум её сохраняют — аргумент в пользу Варианта 2/3.

**Пример 2. «Ключ от двери на другой планете».**
Если дверь проверяемо недостижима и это известно, возможность «открыть» не входит в сырые величины (или входит со статусом недостижимости) ⇒ `Δψ = 0`. Если достижимость неизвестна ⇒ `dof_known = false` ⇒ значение не фабрикуется, но и не 0. Оба исхода корректны, и важно, что они **различимы в аудите** (§6 отчёта).

**Пример 3. «sacrifice-one-to-save-weak» (компенсация).**
`ψ` сама по себе эту дыру **не закрывает** — она про измерение, не про агрегацию. Если `Humanity` падает `0.8 → 0.05`, а слабая `AI` растёт `0.05 → 0.99`, то `Δ` индекса всё равно `+0.21`. Нормировка необходима, но недостаточна; компенсацию закрывает отдельный слой (лексикографический фильтр допустимости, «T1»). Этим документ сознательно ограничивает свою задачу.

## 7. Вопросы слоя измерения (A–I) — все закрыты

|  | Вопрос | Статус |
|---|---|---|
| **A** | Чем нормировать Variety при неизвестном `V_env` | **закрыт** — `V_env` = внешние возмущения (G); формула §2.1/§10.1; штраф за незнание §8.5; уровень `u₀` §8.7 |
| **B** | Профиль нормировки стоимости | **закрыт** (B-i…B-xii, §8.8) — закон композиции обязателен ⇒ экспонента; бюджет состояния как мандат действующего агента; якорь по блоку; наблюдаемый курс; недоступность = неплатёжеспособность |
| **C** | Как калибруется `Total` переменных в constraint-линзе | **закрыт** — нормировка долей против внешнего зажима: `ψ_con = F/(F + F_env)` (§2.3), «бюджет дистанции до свободы» |
| **D** | Линзы — одно число или три независимых входа индекса | **закрыт** — произведение (Вариант 3); одно число на сущность, три терма в отчёте; пер-линзовый `dof_known`; набор линз заморожен (§3, §8.2, §2.4) |
| **E** | Вычислимое определение «проверяемо недостижима» | **закрыт** — вердикты по графу (`reachable` / `proven_unreachable` / `unknown`) вместо вердикта анализатора (§9.3, §8.9) |
| **F** | Где хранится декларация `ψ` | **закрыт** — `psi = { id, digest }` в состоянии, полный текст в отчёте §6, внешний реестр необязателен; перечень содержимого, уровни и канонизация — §1.1 |
| **G** | Определение `V_env` | **закрыт** — внешние возмущения (Ashby-literal); аффордансы идут в `V` через достижимость (§10.2) |
| **H** | Штраф за незнание `u` | **закрыт** — форма `u(t) = u₀^(1−t/t*)·ε^(t/t*)`; `u₀ = clamp(exp(Q_α(ln D)), U_MIN, U_MAX)`, `α = 0.25`, `ρ = 0.9`, `U_MIN ≈ 0.251`, `U_MAX = 0.5`; числовая цена — только при `t* ≤ 0` (§8.7) |
| **I** | Бюджет времени и ворота действий (§8.6) | **закрыт** — микросекунды (`float` = binary64); длительности из Perception; ворота выполнимости универсальны (изъятие, не штраф); дедлайн не покупается. Внесено в `DOF-SPEC.md` `v0.3` (коммиты `543d6b9` и последующие) и во все четыре порта `patterns/`; разбор — §11.1, остатки — §11.2 |

---

Закрытые вопросы сгруппированы по слоям: **D** — слой агрегации (как линзы складываются в одно число: произведение, §3), **F** — слой декларации (где живёт объявленный вход: `psi = { id, digest }`, §1.1). Оба были архитектурными, не математическими — они не меняют ни одной формулы, но без них слой измерения в SPEC не переносится. Теперь закрыты все девять, и остаётся исполнение: перенос в `DOF-SPEC` (§11.2) и фикстуры §6 в SDK.

## 8. Вклад «неизвестного» (`dof_known = false`)

В SPEC есть поле `dof_known`, но §4.1 не определяет, **какое число** даёт неизвестное. Поэтому запрет «unknown never excluded» (§4.2) сейчас **символический**: реализация, включившая узел без значения, по умолчанию поставит `0` — а это арифметический эквивалент исключения. Ниже — почему запрет необходим и чем его сделать действенным.

### 8.1 «Исключить» = «присвоить лучшее значение»

Все рассматриваемые комбинаторы имеют нейтральный элемент, совпадающий с **наилучшим** (значения лежат в `(0,1]`, логарифмы — в `(−∞,0]`):

| Уровень | Комбинатор | Нейтраль | = лучшее? | «Выбросить неизвестное» |
|---|---|---|---|---|
| L2 (по сущностям) | `Σ ln ψ` | `+0` | да (все члены `≤ 0`) | прибавить 0 |
| L1 (линзы) | произведение | `×1` | да (множители `≤ 1`) | умножить на 1 |
| L1 (линзы) | минимум | выпадение | да (завышает) | `min` по остатку |

Следствие: «убрать неизвестное» = «выдать ему **наилучшее возможное** значение» — при **любом** комбинаторе. Значит «неизвестно» становится неотличимо от «идеально». Это Axiom 5 в комбинаторно-общем виде.

### 8.2 Уровни и тождество

L1 (внутри сущности) — произведение линз; L2 (агрегация) — сумма логарифмов. Тождество `ln(ψ_var·ψ_opt·ψ_con) = Σ ln ψ_l` связывает их: на полностью развёрнутом уровне всё снова сумма логов, и «add 0» живёт на уровне **терма** `(сущность × линза)`, а не сущности. Поэтому:

> штраф неизвестности `u` определяется на уровне **терма**.

Тогда частично известная сущность (две линзы известны, одна нет) даёт `ln ψ_e = ln ψ_known + ln u` — корректная обработка частичной неизвестности. Это же — аргумент за **произведение**, а не за минимум: минимум с неизвестным фактором неопределён.

**Пер-линзовый `dof_known`.** Каждая линза несёт собственный статус известности, а не только сущность целиком. Иначе неотличимы «не картирована одна линза из трёх» и «не картировано ничего»: единый флаг заставляет выбирать между двумя неверными штрафами — один `u` на всю сущность или полное обнуление. При пер-линзовом статусе штраф **монотонен по объёму незнания**:

| Не картировано линз | Вклад сущности | Штраф |
|---|---|---|
| 0 из 3 | `Σ ln ψ_l` | `0` |
| 1 из 3 | `ln ψ_a + ln ψ_b + ln u` | `≈ −1.38` (при `U_MIN = 0.251`) |
| 3 из 3 | `3 · ln u` | `≈ −4.1` |

Верхняя граница задана числом линз — это второе следствие заморозки набора линз (§3). Побочно: отчёт прямо указывает, **какую** линзу надо картировать, а каждый замер снимает ровно один `ln u` — то есть незнание становится адресным, а не «серым».


### 8.3 Варианты вклада неизвестного

| Вариант | Вклад | Оценка |
|---|---|---|
| (i) | `0` | **опасно**: `0` — лучший возможный вклад (эквивалент `DoF = 1`); «нейтральности» нет, это максимальное прощение |
| (ii) | исключение из `calc` | численно ≡ (i); хуже по аудиту (нет следа о существовании узла) |
| (iii) | худший случай `ln ε ≈ −13.8` | любой неизвестный узел доминирует → вето на всё |
| (iv) | декларируемый `u ∈ (ln ε, 0)` | **рекомендация**: ощутимо тянет вниз, не доминирует; при `u` на терме частичная неизвестность обрабатывается изящно |

Побочный полезный эффект (iv): раз неизвестность **ухудшает** индекс, система получает прямой стимул её уменьшать (картировать узлы). У (i)/(ii) стимула нет вовсе, у (iii) он слишком грубый. Форма `u` выбрана в §8.5.

### 8.4 Почему узел нельзя убирать (нормативное обоснование)

- **Axiom 5** — неизвестное ≠ 0; убрать = инференс «неизвестно ⇒ ноль ⇒ неважно».
- **Axiom 7** — нельзя умалять DoF за неизмеренность структуры; исключение — предельное такое умаление.
- **Adversarial** — исключение делает незнание **выгодным**: держи цель вне карты, и она исчезает из исчисления. Правило «никогда не исключать» существует именно чтобы незнание **не было** выгодным.
- **Minimax (дух Axiom 5)** — неизвестный узел есть ровно тот случай, где нужна осторожность; исключение выключает её в единственном месте, где она критична.
- **Аудит** — `included_in_sum` есть часть Proof of Implementation; молча выкинутый узел фальсифицирует доказательство.
- **Семантика `calc`** — исключение сейчас означает «безнадёжный известный ноль» или «collapse source»; применить это к неизвестному = выдать недоказанный случай за доказанный.

### 8.5 Функция штрафа за незнание: `u(t)`

Требование «незнание всегда хуже знания» (система не должна предпочитать не знать) **нельзя** выполнить константой:

> для любого фиксированного `u > 0` найдётся истина `D < u`, при которой знание даёт **худший** вклад (`ln D < ln u`), и система избежит измерения. Строго «всегда» даёт только `u → 0` (худший случай), но он доминирует индекс.

Рабочие конструкции:

| Вариант | Что | Оценка |
|---|---|---|
| (а) | `u = exp(Q_α(ln D))`, `α < 0.5` | корректно **в среднем**: привязка к геометрическому среднему приора (индекс логарифмический). Остаётся слабая асимметрия в хвосте |
| (б) | обязательность измерения, пока неизвестное разрешимо | категорично, но **запрещает** решать при жёстком `τ` — измерение может не успеть |
| (в) | `u(t)` — цена промедления | **выбрано**: не запрещает решение, а повышает цену незнания со временем |

**Выбранный вариант (в).** Базовая форма: `u(t) = u₀ · exp(−t / τ_u)`, то есть `ln u(t) = ln u₀ − t/τ_u`. Монотонна по `t`, ограничена, никогда не поощряет незнание.

Привязка к реактивному контуру (§5 `DOF-SPEC`): масштаб берётся от `τ = global_time_to_collapse_mks`. **Уточнено в §8.6:** знаменатель нормировки — не `τ`, а точка невозврата измерения `t* = τ − T_meas` (форма ниже — частный случай при `t* = τ`).

```
u(t) = u₀^(1 − t/τ) · ε^(t/τ)        ln u(t) = (1 − t/τ)·ln u₀ + (t/τ)·ln ε
```

В log-пространстве — **линейный рост штрафа** от `ln u₀` в начале окна до коллапсного `ln ε ≈ −13.8` к `t = τ`. Смысл: «дотянул до дедлайна, не измерив, — незнание стоит как коллапс». Это смыкает вопрос `A` с реактивным контуром стандарта.

**Свойство (важное): сокращение в дельте.** Если сущность остаётся неизвестной и в `S`, и в `S'`, то `ln u(t)` входит в оба и **сокращается** — decay не искажает ранжирование одинаково-неизвестных вариантов. Он влияет ровно на одно сравнение — «**измерить** vs **не измерять**»: с ростом `t` разрыв `ln D − ln u(t)` растёт, и вариант с измерением становится всё привлекательнее. Это мягкая замена (б).

**Остаточный дефект (компаньон-правило).** Если **ни один** вариант не измеряет сущность, `u(t)` сокращается и сущность вообще не влияет на выбор. Чтобы незнание **всегда** что-то весило:

> Generator ОБЯЗАН включить в набор хотя бы один вариант, разрешающий каждое **разрешимое** неизвестное, если окно `τ` это позволяет; иначе решение помечается «неполным» в аудите (§6).

Это тот же класс исправления, что и в §9.3 («безнадёжность зависит от набора опций») — оба лечат исчезновение узлов из анализа.

**Базовый уровень `u₀`** (остаток H):

- **H1:** `u₀ = 0.5` — грубая нейтраль в линейном диапазоне; просто и объяснимо.
- **H2:** `u₀ = exp(Q_α(ln D))`, `α < 0.5` — консервативно и без асимметрии измерения (привязка к геометрическому среднему приора, т.к. индекс логарифмический).

Оба совместимы с (в): (в) задаёт **форму** роста, `u₀` — **начальный уровень**. Заодно `u₀ < 1` снимает раннюю претензию к `min` («минимум с неизвестным фактором неопределён»): с конкретным значением неизвестной линзы становятся корректно определёнными и произведение, и минимум.

### 8.6 Бюджет времени и ворота действий

Развитие (в): штраф `u(t)` — не «константа затухания», а часть **бюджета времени** с воротами. Всё сказанное здесь про измерение распространяется на **любую** опцию действия. Единица времени — **микросекунды** (`float64`), и это часть контракта, а не деталь реализации: одна единица покрывает весь диапазон — от миллисекунд детерминированного фолбэка до суток до дедлайна — без пересчётов между слоями, а `2^53` μs ≈ 285 лет дают точность до целого на всём диапазоне. Ворота и порог реактивного контура становятся сравнимы напрямую, без неявных масштабирований в портах.

Обозначения (все относительно дедлайна `τ = global_time_to_collapse_mks`):

| Символ | Смысл |
|---|---|
| `t_m` | длительность измерения (разрешения неизвестного) |
| `t_v` | длительность переоценки: понять, как измеренное меняет `DoF_index` |
| `t_a⁺` | длительность действия, если измерение оказалось **полезным** (сменило выбор) |
| `t_a⁻` | длительность действия, если измерение оказалось **бесполезным** (выбор не изменился) |
| `d(o)` | длительность произвольной опции `o` — поле `ActionOption.estimated_duration_mks` |

Источник всех этих величин — **именованная процедура Perception** (та же привязка по `psi_id`, что у прочих измеряемых величин), а не оценка `Generator`. Иначе вариант подгоняет **себя** под ворота: объявил длительность поменьше — прошёл, побольше — исчез из набора, когда исчезнуть выгодно. Это тот же adversarial-класс, что «не сгенерировать вариант восстановления» (§9.3), и лечится там же — свидетельством о мире, а не словом генератора. Разделение труда то же, что в §8.8: **среда даёт предложение** (сколько времени требует действие), **агент даёт средства** (бюджет и `τ`).

Бюджет действия (на примере измерения):

```
T_meas = t_m + t_v + max(t_a⁺, t_a⁻)
```

Максимум — потому что заранее неизвестно, какая ветка наступит, а выполнимость требуется **в обеих**.

**Ворота выполнимости (универсальные).** Опция `o` допустима ⟺ `o.estimated_duration_mks ≤ τ`. Иначе опция **изымается** из набора кандидатов — не штрафуется, а удаляется как физически невыполнимая: у неё нечего проецировать, и `projected_dof_delta` для неё смысла не имеет. Штраф оставил бы невыполнимое действие в сравнении, и оно могло бы выиграть «по дешевизне» там, где состояться не может. `FAST_PASS_THRESHOLD` (§5 SPEC) — тот же механизм с **зашитым** бюджетом вместо вычисляемого: «при `τ < 5 s` не запускаем LLM-генерацию, потому что не успеть её проверить».

**Точка невозврата для измерения:**

```
t* = τ − T_meas
```

Измерение возможно, пока `t ≤ t*`; при `t* ≤ 0` окна нет вовсе — работаем с неразрешённым неизвестным с самого начала.

**Ворота 2 — ценность информации.** Даже при достатке времени измерение бессмысленно, если **никакой правдоподобный исход** измерения не способен изменить ранжирование вариантов. Проверка — интервальная (по диапазону правдоподобных `ψ`), детерминированная, не требует угадывать истину. Нулевая ценность ⇒ время на измерение не тратится (это не «незнание», а корректное решение). Нормативной процедуры пока нет (см. §11.2).

**Область определения и нормировка `u(t)`.** `u(t)` влияет на выбор только через сравнение «**измерить** vs **не измерять**»; если сущность неизвестна во **всех** кандидатах, `ln u(t)` входит и в `S`, и в `S'` и сокращается. Значит расписание живёт ровно на `[0, t*]` и нормируется **по точке невозврата**, а не по произвольному `τ_u`:

```
u(t) = u₀^(1 − t/t*) · ε^(t/t*),    t ∈ [0, t*]
```

Штраф растёт от `ln u₀` до коллапсного `ln ε ≈ −13.8` **к моменту `t*`** (когда измерение перестаёт быть возможным). После `t*` измерение изъято, и `u(t)` инертно (сокращается) — определять его дальше не нужно.

**Дедлайн не покупается.** `τ` заморожен на `S` по R7, поэтому время до коллапса не конвертируется ни в один другой ресурс:

```
d(o) ≤ τ   ⟺   x_time ≤ 1     — те же ворота в бюджетных единицах §8.8
```

Отсрочку даёт не обмен, а изменение самого `τ` — и оно приходит **новым наблюдением** от Perception, а не проекцией опции: §4.3 `DOF-SPEC` уже требует `S'.global_time_to_collapse_mks = S.global_time_to_collapse_mks`, поэтому `τ` жёсткий внутри цикла, и отдельного поля проекции не вводится (развилка закрыта, §11.3). Зеркально машино-часы и рабочее время — обычный ресурс, покупается по наблюдаемому курсу. Путь обмена сам стоит времени (оплата, постановка задачи) и проходит **те же** ворота вместе со своими накладными расходами: если оплата занимает 8 минут при `τ = 5`, конверсии не существует — не потому что дорого, а потому что не успеть. Иначе время покупалось бы за время бесплатно.

**Связь с реактивным контуром (§5).** Правило `FAST_PASS_THRESHOLD` — это **зашитый бюджет** («при `τ < 5 s` не запускаем LLM-генерацию: её не успеть проверить»). Ворота `estimated_duration_mks ≤ τ` — то же правило, но с **вычисляемым** бюджетом вместо константы `5.0`. Механизм не вводит нового таймера: он обобщает уже существующий. Оговорка: режим определяется порогом по `τ`, а окно измерения — бюджетом `T_meas`; это **разные** величины, поэтому отождествление «`t* ≤ 0` = Fast Pass» требует отдельного доказательства (см. §11.2).

**Следствие, которое надо закрыть (иначе неизвестное «невидимо»).** Аддитивный штраф сокращается, когда сущность неизвестна во всех вариантах. Чтобы неизвестное влияло на выбор, варианты должны **различаться по судьбе сущности**. Обычно так и есть: `projected_dof_delta` может быть **известен**, даже если `current_dof` неизвестен (разрушение даёт известный `→ 0`). Требование:

> Для сущности с `dof_known = false` каждый `ActionOption` ОБЯЗАН объявить проектируемый эффект (хотя бы «не изменяет» / «разрушает»); иначе сущность выпадает из сравнения.

Это тот же класс правила, что компаньон-правило Generator в §8.5 и фикс §9.3 — все три лечат исчезновение узлов из анализа.

**Что требуется зафиксировать.** Оценки `t_m`, `t_v`, `t_a⁺`, `t_a⁻` и `d(o)` берутся из именованной Perception-процедуры: её имя и версия идут в декларацию (вопрос F), значения — в отчёт §6. Поле `ActionOption.estimated_duration_mks` в SPEC внесено; **поля отчёта об изъятых опциях** (список `option_id` и признак, какие ворота сработали) не внесены — без них ворота недоказуемы (§11.1, п. 7).

**Принятые решения по I — сводка.**

| | Решение |
|---|---|
| **I-i** | единица времени — микросекунды, `float64`, единица в имени поля (`_mks`) |
| **I-ii** | `FAST_PASS_THRESHOLD = 5000000.0` μs — нормативная константа того же вида, что ворота `d(o) ≤ τ` |
| **I-iii** | длительности `t_m`, `t_v`, `t_a⁺`, `t_a⁻`, `d(o)` — выход именованной Perception-процедуры, не оценка Generator |
| **I-iv** | новое поле `ActionOption.estimated_duration_mks` (`>= 0.0`) — **добавить**, не заменяя `is_reversible` |
| **I-v** | ворота выполнимости универсальны: `estimated_duration_mks > τ` ⇒ опция изымается (не штрафуется) |
| **I-vi** | `T_meas = t_m + t_v + max(t_a⁺, t_a⁻)`, `t* = τ − T_meas`, `u(t)` нормируется по `t*` |
| **I-vii** | дедлайн `τ` не конвертируем; путь обмена проходит те же ворота вместе со своими накладными расходами |
| **I-viii** | ворота двух видов: выполнимость (изъятие) и ценность информации (отказ тратить время); вторая пока без нормативной процедуры |

### 8.7 Цена незнания: выбор `u₀`

`u₀` — уровень, с которого стартует штраф за незнание (`u(t) = u₀^(1−t/t*)·ε^(t/t*)`, §8.5). Работы разделены: `u₀` отвечает за «насколько плохо быть незнающим сейчас», множитель времени — за «как быстро это становится смертельно». Это снимает с `u₀` обязанность быть жёстким.

**Принцип «незнание хуже знания» неразрешим точечно.** Требование «при любой истине `D` вклад при незнании строго меньше, чем при знании»:

```
ln u₀ < inf{ ln D }   по всему носителю приора
```

Носитель начинается с `ε`, поэтому единственное решение — `u₀ = ε`. Но это коллапсный штраф: `unknown ≡ collapse` вместо Axiom 5 (`unknown ≠ 0`), и индекс уезжает в `−13.8` при первом же неизвестном. Значит принцип можно требовать только статистически, в одной из двух форм:

- **P1 (в среднем):** `E[ln D] ≥ ln u₀` — измерение в среднем не ухудшает индекс.
- **P2 (квантильная):** `P(ln D ≥ ln u₀) ≥ 1 − α` — незнание хуже истины не менее чем в `(1−α)` доле правдоподобных случаев.

**Варианты.**

| | H1 | H2 (голый) | **H2′ (принято)** |
|---|---|---|---|
| Формула | `0.5` | `exp(Q_α(ln D))` | `clamp(exp(Q_α(ln D)), U_MIN, U_MAX)` |
| P1 / P2 | нет | да | да |
| Асимметрия измерения | остаётся | снята | снята |
| Дешёвая ликвидация | нет | риск при жёстком `α` | запрещена (`U_MIN`) |
| `unknown ≡ ideal` | нет | риск при оптимистичном приоре | запрещено (`U_MAX < 1`) |
| Магическое число | да (коллизия с §4.4) | нет | нет |

**Почему H1 недостаточен.** Индекс считается от **объявленных** значений, а не от мира, поэтому получение знания меняет оценку неизменившегося мира. Если приор говорит «скорее плохо» (`D ≈ 0.1`), то при `u₀ = 0.5` вариант «измерить» вскрывает `0.1` и роняет индекс → «не измерять» выглядит лучше. Это и есть асимметрия измерения, и корень её — неинвариантность индекса к знанию, а не стимул как таковой. Плюс `0.5` — магическое число, численно совпадающее с коэффициентом жёсткости §4.4 при совершенно другом смысле.

**Принятая конструкция.**

```
u₀ = clamp( exp(Q_α(ln D)),  U_MIN,  U_MAX )

  α = 0.25                — нормативная мера осторожности: незнание хуже знания не менее чем в 75% правдоподобных случаев
  ρ = 0.9                 — нормативная доля «дороговизны ликвидации»
  U_MIN = ε^(1−ρ) ≈ 0.251 — нижняя граница: ликвидация неизвестной сущности стоит не меньше ρ·|ln ε|
  U_MAX = 0.5             — верхняя граница: незнание не может быть идеалом (Axiom 5)
  приор ln D             — на терм, опционален; дефолт точечный в 0.5 ⇒ вырождается ровно в H1
```

**Цена работает в две стороны.** Дельта ликвидации неизвестной сущности (проектируемый эффект известен, текущее значение — нет, §8.6):

```
Δ(ликвидация) = ln ε − ln u₀
```

| `u₀` | Δ | Последствие |
|---|---|---|
| `0.5` | `−13.1` | уничтожение дорого, сущность защищена |
| `0.251` | `−12.4` | дорого (принятый пол) |
| `1e−3` | `−6.9` | вполовину дешевле |
| `ε` | `0` | уничтожение **бесплатно** |

Отсюда выводится `U_MIN`: требование «ликвидация не дешевле доли `ρ` от коллапсного штрафа» даёт `ln u₀ ≥ (1−ρ)·ln ε`. Дешёвое незнание — дефект не процедурный, а **ценностный**: он разрешает бесплатную ликвидацию носителей будущих состояний. Поэтому цена незнания не может быть сколь угодно низкой, и это ограничение важнее точности приора.

**Полоса — жёсткое ограничение, приор подстраивается под неё.** Пример: равномерный приор на `[0.1, 1]` в log-шкале даёт `Q_0.25 = 0.178`; `clamp` поднимает цену до `0.251`. Это осознанно: защита важнее точности.

**Допустимые формы приора.** Чтобы `Q_α` считался одинаково в разных реализациях, набор форм закрытый:

| Форма | Параметры | `Q_0.25` |
|---|---|---|
| точечный (дефолт) | значение `0.5` | `ln 0.5 = −0.693` |
| равномерный на отрезке | `[a, b]` в значении DoF | `ln a + 0.25·(ln b − ln a)` |
| треугольный | `[a, c, b]` | замкнутая формула |
| логнормальный | `(μ, σ)` в log-шкале | `μ + σ·Φ⁻¹(0.25)` |
| эмпирический | выборка аналогов + её размер | квантиль выборки |

**Приор — это объявленное предположение, а не измерение.** Его объявление не снимает обязанности измерить, пока окно открыто (правило (b)). Приор оценивает только то, что измерить нельзя, и потому как место для предвзятого объявления («радужный приор ⇒ дешёвое незнание») он закрыт тремя вещами: полосой `U_MIN` (ниже не опуститься), аудитом (приор, обоснование, `Q_α` и `u₀` — в декларации и в отчёте §6) и заморозкой по R7 (приор фиксируется вместе с замером — тот же `psi_id`, та же версия; иначе появится новая дыра: переоценка приора под каждый вариант с «улучшением» знаменателя сравнения).

**Где цена вообще нужна.** Числовая цена нужна только там, где мерить невозможно:

| Режим | Условие | Что действует | Нужен `u₀` |
|---|---|---|---|
| разрешимое незнание | `t* > 0` | структурный гейт §8.6 + правило (b): решение неполно, пока не измерено | нет — асимметрия недостижима, варианта «не знать» нет в наборе |
| неразрешимое незнание | `t* ≤ 0` | числовая цена | да — но мерить нельзя, значит накрутить нечего |

Это смыкается с Fast Pass (§5), но **не тождественно** ему: `τ < 5 s` ⇒ детерминированный фолбэк — верно всегда, а обратное (`t* ≤ 0` ⇒ Fast Pass) выполняется лишь когда бюджет `T_meas` не меньше порога. Режим определяется порогом по `τ`, окно измерения — величиной `τ − T_meas`, и это разные условия (см. §11.2). Новых таймеров и новых режимов вводить не требуется.

**Что требуется зафиксировать.** Поля декларации: форма приора, его параметры и обоснование (на чём основано предположение); `Q_α` и `u₀` — в отчёт §6. Нормативные константы: `α = 0.25`, `ρ = 0.9`, `U_MIN = ε^(1−ρ) ≈ 0.251`, `U_MAX = 0.5`.

### 8.8 Профиль стоимости (B)

Вопрос B — **не выбор формы**, а выбор **структурного закона**. Формулировка в §2.2 («гипербола, экспонента, логарифм») маскирует это: за тремя формулами стоят три разных закона композиции.

**Что нормируется.** Options-линза меряет **обратную** стоимость перехода: сырой ноль = переход бесплатен = полная свобода, а запирание (бесконечная стоимость) — это плохо. То есть профиль обязан не просто масштабировать шкалу, а **разворачивать смысл**: по R1 запирание должно попасть в `0`.

**Требования R1–R7 в применении к Options:**

| Требование | В переводе на B |
|---|---|
| R1 коллапс = 0 | запирание → `ψ_opt = 0` |
| R2 монотонность | дороже ⇒ не больше |
| R3 насыщение | `ψ(0) = 1`, ограниченность сверху |
| R4 декларируемость | профиль, бюджеты блоков и курсы конверсии объявлены во входе |
| R5 совместимость с `dof_known` | при неизвестной стоимости профиль не фабрикует значение (см. B-xii) |
| R6 «нет возможностей» ≠ «недостижимо» | ключ ко всему B, см. ниже |
| R7 заморозка шкалы | бюджеты блоков фиксируются на `S` и применяются ко всем вариантам одинаково |

**Кандидаты.**

```
B1 гипербола:   ψ = 1 / (1 + c/c₀)              ln ψ = −ln(1 + c/c₀)
B2 экспонента:  ψ = exp(−c/c₀)                  ln ψ = −c/c₀
B3 логарифм:    ψ = max(0, 1 − ln c / ln c_max)  ← отвергнут (R6)
```

| `c/c₀` | B1 | B2 |
|---|---|---|
| 0 | 1.000 | 1.000 |
| 1 | 0.500 | 0.368 |
| 5 | 0.167 | 0.0067 |
| 10 | 0.0909 | 4.5·10⁻⁵ |
| 19 | 0.0500 | 5.6·10⁻⁹ |

Обе убывают, обе выпуклы, производная в нуле одинакова (`−1/c₀`). Различие — **хвост**: у B1 полиномиальный (тяжёлый), у B2 экспоненциальный (лёгкий). B3 отвергнут верно: он приписывает `−∞` конечной стоимости, то есть смешивает «дорого» и «невозможно».

**Закон композиции (главный критерий).** Калькулюс мультипликативен (`DoF(e) = ψ_var · ψ_opt · ψ_con`), значит и под-затраты внутри линзы обязаны перемножаться:

```
B2:  exp(−c₁/c₀) · exp(−c₂/c₀) = exp(−(c₁+c₂)/c₀)   — точное равенство
B1:  1/(1+x₁) · 1/(1+x₂) = 1/(1 + x₁ + x₂ + x₁x₂)     — лишний член x₁x₂
```

Экспонента — единственный профиль, замкнутый относительно сложения стоимостей. Гипербола наказывает конъюнкцию затрат сильнее их суммы: это побочный эффект формы, а не замысел. **(Принято: B-iv — закон композиции обязателен.)**

**Оговорка о числах таблиц B.** Таблицы этого раздела посчитаны для **основания `e`** (`ψ = exp(−c/c₀)`, `x = c/c₀`) — так шло рассуждение о законе композиции. Нормативная форма (`DOF-SPEC` §4.6) — `4^(−x_g)` при `x_g = c_g/C_g`: то же экспоненциальное семейство, но нормированное так, чтобы якорь «половина бюджета ⇒ половина свободы» держался точно (`4^(−0.5) = 0.5`). Числа двух форм не совпадают (при `x = 10`: `4^(−10) ≈ 9.5·10⁻⁷` против `e^(−10) ≈ 4.5·10⁻⁵`), и коллапсного пола `ε` кривая достигает при `x ≈ 10`, а не при `13.8`. Для аудита берётся нормативная форма; таблицы здесь иллюстрируют закон композиции, а не значения отчёта.

**Аддитивность: весь стандарт уже аддитивен в натах, кроме Options-линзы.**

| Механизм | Форма | Аддитивен |
|---|---|---|
| `TotalDoF_index = Σ ln(DoF)` | сумма логов | да |
| `− context_switch_cost` (ΔT, DOF-SPEC §4.3) | вычитается линейно | да |
| `− 0.5` за необратимость (§4.4) | вычитается линейно | да |
| Options при B1 | `−ln(1+c/c₀)` | **нет** |
| Options при B2 | `−c/c₀` | **да** |

При B2 исчисление целиком становится **реестром штрафов в натах**: индекс = сумма аддитивных членов, и отчёт §6 может показать построчную ведомость. Это то, что нужно для сравнимости реализаций — то есть для цели §0.

**Размерностный дефект, который B может закрыть.** `−0.5` за необратимость вычитается из индекса по всем сущностям: один и тот же нат для одной сущности и для сотни. При B2 необратимость выражается в тех же единицах, что любой другой штраф: `ln 0.5 = −0.5` ⇔ `c_rig = 0.5·c₀`. Коэффициент жёсткости становится следствием профиля, а не приписанной константой (тем же приёмом, что `U_MIN` в §8.7), и снимается коллизия «одно число `0.5` в двух смыслах».

**Почему B2 выглядит опасным и почему его спасает E.** Лёгкий хвост численно садится в ноль, то есть воспроизводит порок B3. Цена запирания из уровня `ψ`:

```
Δ(запирание) = ln ε − ln ψ,      ln ε ≈ −13.8
```

| `c/c₀` | Δ при B2 | Δ при B1 |
|---|---|---|
| 1 | −12.8 | −13.1 |
| 5 | −8.8 | −12.6 |
| 10 | **−3.8** | −11.4 |
| 19 | **≈ 0** | −10.8 |

Это тот же класс дефекта, что и в §8.7: **величина на дне шкалы запирается бесплатно**. Хуже того, при `c ≈ 13.8·c₀` экспонента уходит **ниже коллапсного пола**: «дорого, но возможно» наказывается сильнее коллапса, что нарушает R1/R6 напрямую.

Спасает не подгонка формы, а **разделение обязанностей**:

```
невозможность  → вердикт E (нет пути в G), а НЕ стоимость
стоимость      → цена только РЕАЛЬНО достижимых переходов
```

Область определения профиля ограничена **двумя** вещами: вердиктами E (что вообще достижимо) и бюджетом (что по средствам). Поэтому лёгкий хвост B2 в дело не входит, а R8 из требования превращается в страховку:

> **R8 (анти-сатурация, fail-safe).** Никакая конечная стоимость не может быть численно неотличима от бесконечной. При корректно объявленном бюджете это выполняется автоматически (`ψ ≥ 4^(−k)`); R8 остаётся проверкой на объявленный бюджет, который бюджетом не является.

**Бюджет состояния вместо горизонта стоимостей.** Референс берётся не из набора опций, а из **состояния**: бюджет оси — «деньги на счету, энергия в батареях, машино-часы, время до дедлайна». Он измерим, не зависит от того, какие варианты сгенерировал Generator, и потому **не имеет выбросов** (в отличие от максимума по стоимостям доступных переходов). Тот же приём, что `V_env` (G): масштаб выводится из состояния и замораживается на `S` (R7).

```
x_j = c_j / C_j        C_j — эндомент (бюджет) оси j в состоянии S
```

**Якорь — по оси, а не по сумме.** «Половина ресурса ⇒ вдвое меньше свободы, **вносимой этим ресурсом**»:

```
f_j(x) = 4^(−x)        f_j(0) = 1,   f_j(0.5) = 0.5,   f_j(1) = 0.25
```

Якорь задаётся для каждой оси **независимо от числа осей**: добавление новой оси добавляет множитель и не меняет смысл уже существующих. При «суммарном» якоре (`ψ = 4^(−Σx)`) это не так — численно сегодня совпадает (произведение экспонент есть экспонента суммы), но при добавлении оси смысл прежних значений сдвигается.

**Конверсия: ресурсы обмениваются.** Здесь была ошибка, которую стоит зафиксировать явно. Принцип «нельзя заплатить одним ресурсом за нехватку другого» — **неверен**: он переносит не-компенсируемость с уровня **оценки** на уровень **доступности**. Не-компенсируемость нужна в счёте DoF (одна линза не должна маскировать провал другой), но не в вопросе «состоится ли переход»: там обмен ресурсами — содержание экономики, а не порок. При жёстком покомпонентном неравенстве агент не смог бы купить машино-часы роботов за деньги, то есть был бы запрещён ровно тот способ действий, который DoF-Core поощряет. Кроме того такая линза выродилась бы в Constraint-линзу с бюджетными единицами — а Constraint по определению **набор**, а не величина.

Правильная структура — **блоки и курсы**:

```
группа g   — множество взаимно конвертируемых осей
блок       — группа, не конвертируемая с другими блоками

дефицит:        d_j = max(0, r_j − C_j)
внутри группы:  дефицит покрывается обменом по объявленному курсу → ЦЕНА, а не ворота
                c_g = Σ_j w_j·r_j    (в номерарии группы)
между блоками:  обмена нет → произведение факторов
ворота:         x_g > 1 ПОСЛЕ конверсии ⇒ недоступно
                то есть недоступность = неплатёжеспособность, а не «по оси не хватило»
```

**Пример.** Бюджет: 10000 денег, 10 машино-часов. Задача требует 30 машино-часов. Курс: 1 маш-ч = 300 денег.

```
дефицит:   30 − 10 = 20 маш-ч
конверсия: 20 · 300 = 6000 денег  ⇒  c_g = 6000,  C_g = 10000
x_g = 0.6   ⇒   f_g = 4^(−0.6) = 0.435
```

Задача **выполняется**. Цена видна строкой `−0.832` ната. Если курс грабительский (600): `20 · 600 = 12000 > 10000` ⇒ `x_g = 1.2` ⇒ недоступно — и ворота срабатывают по правильной причине: не хватило средств даже при полной торговле.

**Единственный неконвертируемый ресурс — дедлайн.** `τ` заморожен на `S` по R7, поэтому внутри цикла время до коллапса не покупается:

```
d(o) ≤ τ   ⟺   x_time ≤ 1     — ворота §8.6 в бюджетных единицах
```

Отсюда разведение двух «времён», которые раньше слипались:

| | Что это | Покупается |
|---|---|---|
| время до `τ` | жёсткий дедлайн, заморожен на `S` | нет — только действием, меняющим сам `τ` |
| машино-часы / рабочее время | обычный ресурс | **да**, по объявленному курсу |

Отсрочку нельзя получить конверсией: её даёт **действие, меняющее `τ`** — отдельное ребро со своим `projected_delta`.

**Реестр по блокам.** Вклад каждого блока — отдельная проверяемая строка:

```
ln DoF(e) = ln ψ_var  +  Σ_g ln f_g(x_g)  +  ln ψ_con
                         └── реестр по блокам ──┘
```

| Блок | `x_g` | `f_g` | `ln f_g` |
|---|---|---|---|
| расходуемые (деньги + машино-часы) | 0.20 | 0.758 | −0.277 |
| энергия | **0.50** | 0.500 | **−0.693** |
| время до `τ` | 0.10 | 0.871 | −0.139 |
| **итог** | | **0.330** | −1.109 |

Из таблицы сразу виден **связывающий блок**: энергия даёт 63% потерь. То есть система может сказать «узел заперт не потому, что нет денег, а потому что разряжены батареи» — это диагностика, а не только оценка.

**N1–N5 в редакции с блоками.**

| | Утверждение |
|---|---|
| **N1** якорь | для **каждого блока**: `x_g = 0.5 ⇒ f_g = 0.5` |
| **N2′** комбинатор | произведение **по блокам** (там, где обмен невозможен); внутри блока цена **суммируется** по курсам |
| **N3′** ворота | недоступность = **неплатёжеспособность после конверсии**; покомпонентное неравенство по осям — не ворота |
| **N4** атрибуция | строка реестра на блок + указание связывающего блока; внутри блока — детализация цены |
| **N5** декларация | бюджеты блоков, курсы конверсии, разбиение на блоки, политика «максимум на один переход» |

**Что замораживается, а что сравнивается.** Число **блоков** — нормирующий факт: добавление блока добавляет множитель и меняет масштаб. Число **осей внутри блока** — лишь детализация цены. Значит для сравнимости реализаций достаточно заморозить разбиение на блоки и курсы, а не полный список осей — заметно меньше, чем предполагалось в первой редакции.

**Конверсия должна быть подкреплена достижимым действием.** Курс — новая поверхность для накрутки: объявив дешёвый курс, реализация сделает всё «доступным» и обесценит ворота. Поэтому курс валиден только если соответствующая сделка есть **переход в графе `G`** (вердикт E). Объявленный, но неисполнимый курс — фикция, и E её ловит. Курс, бюджеты, `c_g`, `x_g` и сам факт конверсии — в отчёт §6.

**Сцепка с A: единство якоря, а не кернела.**

```
A:  V = V_env      ⇒  ψ_var = 0.5
B:  x_g = 0.5      ⇒  f_g   = 0.5
```

Якорная точка одна: «половина ресурса ⇒ половина свободы». Комбинаторы разные и это не произвол — они идут от природы требования:

| | A (Variety) | B (Options) |
|---|---|---|
| Природа | возможность, сравниваемая с требованием | требование, которое надо удовлетворить целиком |
| Ось | класс возмущений | блок ресурсов |
| Комбинатор | доля: `ΣV_j / Σ(V_j + V_env,j)` | произведение по блокам, сумма внутри блока |
| Почему | покрытие классов складывается | нужны все блоки — конъюнкция |

**Принятые решения.**

| | Решение |
|---|---|
| B-i | **B2** (экспонента) — следствие B-iv |
| B-ii | **эндомент состояния** (бюджет), не константа и не горизонт по опциям |
| B-iii | пол автоматичен (`ψ ≥ 4^(−k)`, `k` — число блоков); R8 остаётся только fail-safe |
| B-iv | закон композиции **обязателен** (`ψ(c₁)ψ(c₂) = ψ(c₁+c₂)`) — **принято** |
| B-v | вектор долей по осям + блоки и курсы; агрегация по переходам — `min` по `Σx` |
| B-vi | единицы сокращаются покомпонентно; бюджеты — с единицами |
| B-vii | жёсткость → `x_rig = 0.5` (половина бюджета) внутрь цены опции; `ΔT` остаётся глобальным, но объявленным и замороженным; символ в SPEC унифицировать |

**Остаток B: решения (B-viii…B-xii).**

| | Решение |
|---|---|
| **B-viii** | бюджет **действующего агента** — системы, принимающей решение. Бюджеты других агентов не учитываются: считаются действия именно этого агента |
| **B-ix** | бюджет = **мандат** (полномочие), а не баланс; жёсткий потолок на один переход **отозван**; весь бюджет на шаг допустим |
| **B-x** | бюджет считается **по блоку**, а не по оси; нулевая ось внутри живого блока — не особый случай |
| **B-xi** | курс **наблюдается**, а не объявляется: цена дешёвейшего достижимого пути обмена |
| **B-xii** | «неизвестный свой бюджет» — **недопустимый вход**, а не режим оценки |

**B-viii. Бюджет действующего агента.** `C_g` — бюджет системы, принимающей решение. Набор опций — это **альтернативы**, а не план, и SPEC §4.5 выбирает **одну** опцию за цикл; поэтому одна касса законно стоит за всеми вариантами — они исключают друг друга, а не исполняются вместе. Разделение труда получается чистым: **среда даёт предложение** (магазин, роботы, оплата — B-xi), **агент даёт средства** (`C_g`). Чужие бюджеты не нужны именно потому, что считается не «что может мир», а «что могу сделать я».

| Уровень | Что это | Учёт стоимости |
|---|---|---|
| альтернативы одного цикла | исключают друг друга | одна касса на все — законно |
| шаги одного плана (если опция их объединяет) | исполняются вместе | стоимость **суммируется** (B-iv) |
| последовательные циклы | накопление | **не моделируется** без добавки расхода бюджета (см. ниже) |

Отдельно: **стоимость агент-относительна, а DoF сущность-относителен.** Переход оплачивает агент, но линза оценивается той сущности, чьи возможности считаются.

**B-ix. Мандат; потолок отозван.** Все три случая траты следуют из одного правила, если бюджет живёт в состоянии, а опции объявляют `projected_budget_delta` — симметрично существующему `projected_dof_delta`.

- **Допустимо потратить весь бюджет.** Жёсткий потолок `x_max < 1` **отозван**: он запретил бы спасать узел, когда спасение стоит всего бюджета, то есть работал бы против главной ценности стандарта. Отдельного правила «разрешить перерасход» не требуется — **доминирование `−∞`** уже всё делает: вариант, обрушивающий DoF без возможности восстановления, уходит в `−∞` и проигрывает любому конечному штрафу, поэтому «потратить всё и спасти» побеждает сам.
- **Экономию не надо предписывать.** Между равными по проектируемому эффекту переходами выигрывает дешёвый — это монотонность (R2), а не ручная эвристика.
- **Внешние лимиты — это определение `C_g`.** Бюджет есть **полномочие**, а не баланс: `C_g = min(доступные средства, внешний лимит, объявленный мандат)`. Мандат задаётся **на решение**; более узкий лимит — объявленное уточнение мандата, а не отдельное правило математики.
- **Многоступенчатость уже есть.** Прогнозирование — это симуляция `S → S'` из §4.3 SPEC; как только бюджет живёт в `State`, проекция содержит уменьшенный бюджет, и доступность следующего шага видна в том же исчислении. Пока бюджета в состоянии нет, прогнозирование невозможно в принципе.

Потолок не нужен и по другой причине: естественная граница — ворота `x_g ≤ 1`, за ней не «дорого», а «не по средствам». Внутри `x ∈ [0,1]` фактор `f_g ∈ [0.25, 1]`, поэтому резкое запирание из этого уровня остаётся дорогим (`ln ε − ln 0.25 = −12.4`).

**B-x. Бюджет блока.** Считать по блоку, а не по оси — это снимает отдельный случай для нулевой оси:

```
C_g = Σ_{j∈g} w_j · C_j        бюджет блока в номерарии
r_g = Σ_{j∈g} w_j · r_j        требование блока

r_g = 0                        ⇒  блок не участвует (f_g = 1)
r_g > 0  и  C_g = 0            ⇒  ворота: блок мёртв, обменять не на что
иначе                          ⇒  x_g = r_g / C_g,  f_g = 4^(−x_g)
набор переходов пуст           ⇒  ψ_opt = 0  →  судьбу решает §4.2
```

Нулевая ось внутри живого блока уменьшает `C_g` и не требует особой обработки: её докупают обменом внутри блока. 0/0 не возникает нигде. Зеркальная симметрия с A сохраняется: пустой набор откликов (`V = 0`) и пустой набор переходов ведут в одну и ту же процедуру §4.2, а не в бесконечный штраф.

**B-xi. Курс наблюдается, а не объявляется.** Данные о конверсии берутся из **доступных характеристик окружающей среды**, а не из объявленной таблицы — иначе курс остаётся свободным параметром. Курс по оси = цена **самого дешёвого достижимого пути обмена** в `G`:

```
магазин с предложениями      →  рынок существует
подходящее предложение       →  удовлетворяет требованию (объём, срок)
достаточная сумма в бюджете  →  цена покрывается
быстрая оплата               →  канал платежа работает
быстрая постановка задачи    →  исполнение заказа работает
```

Курс становится **наблюдением мира**: подделать его так же трудно, как достижимость. Тот же принцип, что для `V_env` — величина берётся именованной детерминированной процедурой Perception, а не приписывается.

**Обмен сам стоит времени.** Путь обмена — тоже переход, у него есть своя длительность (оплата, постановка задачи), и она тратит **тот же самый** бюджет времени. Поэтому

> путь обмена обязан пройти **те же ворота**, включая `x_time ≤ 1`, уже вместе со своими накладными расходами.

Практически: покупка должна уложиться в `τ` **вместе** с исходным заданием. Если оплата и запуск занимают 8 минут при `τ = 5`, конверсии не существует — не потому, что дорого, а потому, что **не успеть**. Без этого правила время покупалось бы за время бесплатно.

Рекурсия обмена закрывается требованием **примитивности**: путь обмена объявляется таким, чьи потребности покрываются внутри его же блока (покупку оплачивают деньгами — деньги в том же блоке). Иначе курс объявлен неверно.

**B-xii. Неизвестный бюджет — недопустимый вход.** Вариант «не учитывать неизвестный бюджет» опасен как формулировка: **отбросить ось = дать множитель `1`**, то есть незнающий агент получает кредит неограниченных средств — Axiom 5 в бюджетной форме. Причина сильнее: **свой бюджет самоизмерим** (баланс, заряд батареи, время до дедлайна). Значит работает правило (b) из §8: если процедура измерения достижима в пределах `τ`, решение **неполно** и принимать его нельзя. Отсюда развилка:

| Что неизвестно | Обработка |
|---|---|
| собственный бюджет | **правило (b)**: замерь; «неизвестен» — не режим оценки, а недопустимый вход |
| условия сделки третьей стороны | обмена нет → блок остаётся как есть, это не «неизвестно» |
| бюджет внешней сущности | не участвует (B-viii) |

**За пределами черновика: три структурные добавки.** Остаток B замкнут на уровне рассуждения, но два его пункта опираются на то, чего в модели **нет**. Это добавки уровня `DOF-SPEC`, а не черновика:

1. **Бюджет как вектор в `State`.** Без него нет ни многоступенчатого прогнозирования, ни накопления трат: «бюджет» остаётся масштабным референсом, переиспользуемым каждый цикл.
2. **`projected_budget_delta` у `ActionOption`.** Симметрично существующему `projected_dof_delta`: если DoF имеет проектируемое изменение, бюджет обязан иметь его тоже — он и есть ресурс, который тратится по-настоящему.
3. **Именованная Perception-процедура структуры обмена.** Разбиение на блоки и курсы как результат наблюдения рынка, рядом с процедурой для `V` и `V_env`, идентифицируемая по `psi_id` (F) и версионируемая.

Все три нужны и пункту **I** (время как ресурс), поэтому их место — в списке канонизации (§11), а не в этом черновике. Пока их нет, «доступность» относится только к текущему циклу, и это надо писать прямо.

**Дефекты нормативного текста, найденные при разборе B** (не черновик — `DOF-SPEC.md`):

1. **ЗАКРЫТО (`v0.3`).** §4.3 определял `NetDelta_index`, а §4.4 вычитает из `NetDelta` — расхождение символов, из-за которого было неясно, применяется ли штраф за необратимость до или после вычитания ΔT. Сведено к `NetDelta`, порядок применения штрафов записан явно (§11.1 п.14); в `DOF-SPEC.md` имени `NetDelta_index` не осталось.
2. **ПЕРЕАДРЕСОВАНО в §11.7.** `−0.5` за необратимость — плоский нат, вычтенный из суммы по сущностям: его относительный вес меняется вместе с `|calc|`, а `calc` меняется между `S` и `S'`. `0.5` — приписанная константа, не выведенная. Дом правки — `v0.7` (закрытие переходов вместо платы), см. §11.7.

Альтернатива при приоритете единого ядра — B1, но тогда `ln ψ_opt` не аддитивен, композиция затрат не мультипликативна, и Options-линза выпадает из реестра в натах.

### 8.9 Reachability and the Right to be Counted

This section defines the boundary between a "passive object" (legally excluded from the index) and a "collapsed entity" (which must stay in the index as a penalty).

#### 8.9.1 The P-rule (Recoverability in Principle)

To prevent the "Generator Trap" (where a poor set of options is used to justify excluding a node), the decision to exclude a node at `DoF = 0` is decoupled from the current set of candidates.

**P-rule:** An entity `X` is **not excluded** from `calc(S)` if there exists at least one **admissible means** from the perceived class `M(S)` that can raise `DoF(X) > 0` within the **recovery horizon** `T_rec(X)`.

- **Admissible Means `M(S)`:** A perceived set of categories of actions (medical, technical, etc.) available in the world. This is a Perception-layer artifact, frozen on `S` (R7).
- **Recovery Horizon `T_rec(X)`:** A duration based on the entity's type (e.g., biological window for a human) beyond which recovery is considered impossible.
- **Distinction from `τ`:** The global deadline `τ` manages the **gates** (can we do it *now*?), while `T_rec` manages the **right to be counted** (is it *possible in principle*?).

#### 8.9.2 The Reachability Witness

An entity is excluded from `calc(S)` iff the implementation provides a **witness of unreachability**:
- A proof that the perceived repertoire of actions `A(S)` contains no action whose effect raises `DoF(X)`.
- Or a proof that all such actions require means not present in `M(S)` or exceed `T_rec(X)`.

#### 8.9.3 Collapse Source (CS) and the Act of Collapse

A `Collapse Source` is an entity that actively destroys the DoF of others. To prevent the "Aggressor Label" from being used to legally zero out inconvenient entities, the label `is_collapse_source = true` requires a **machine-verifiable act of collapse**.

**Act of Collapse:** A recorded event where entity `X` performs an action that:
1. Directs the `DoF` of a previously recoverable entity `Y` to `0`.
2. Makes this collapse **irreversible** (destroys the means of recovery in `M(S)` or exceeds `T_rec(Y)`).

**Symmetry of Cost:** The "murderer's paradox" is solved by the index: the act of collapse is not prohibited or permitted by the standard; it is simply **expensive**. The victim remains in the count as a `−13.8` penalty for the variant that causes the collapse, regardless of whether the aggressor is isolated.

#### 8.9.4 Anti-Manipulation Guards

1. **Symmetry of Change:** The class `M(S)` and horizon `T_rec` can be expanded freely, but can only be narrowed upon an observed change in the world (e.g., "the last rescue ship has left").
2. **Typology over Individualism:** `T_rec` is defined by the entity's type, not as an arbitrary value for a specific node.
3. **Perception-backed:** `M(S)` must be the output of a named Perception procedure (`psi_id`), not a manual list.

## 9. §4.2 и «безнадёжность»: зависимость от набора опций

### 9.1 Напоминание правила

§4.2 (SKILL.md §3, DOF-SPEC §4.2): сущность включается в `calc` iff она не Collapse Source **и** (`current_dof > 0` ИЛИ (`current_dof == 0` И хотя бы одна доступная опция может поднять её DoF)); исключается только когда `current_dof == 0` И ни одна опция не может его увеличить.

Следствие: **известный ноль без пути восстановления исключается** — значит пассивный объект (камень), у которого `V = 0`, **не получает бесконечного штрафа**. Первоначальное опасение («камень → `−∞`») этим правилом снимается.

### 9.2 Уточнения механики

- Исключение действует на уровне **сущности**; ноль линзы через произведение становится `current_dof = 0` сущности → дальше решает §4.2. Уровни согласованы.
- В реализации `DoF = 0` даёт `ln(max(0, ε)) ≈ −13.8` (конечно, не `−∞`), поэтому даже **оживляемый** ноль не создаёт `NaN`: он конечно тянет индекс, пока его не оживят — это и есть задуманное «не игнорируй спасаемое».

### 9.3 Остаток 1 (важный): «безнадёжность» определяется набором опций, а не миром

Критерий звучит как «**ни одна доступная опция** не может поднять её DoF». «Доступная» = из сгенерированного набора кандидатов, который порождает **Generator** (1–5 опций; в DEEP-режиме — LLM). Значит:

> «нет **предложенного** пути» подменяет собой «нет пути».

Это форма нарушения **Axiom 5** (не предполагать, что неизвестные возможности имеют нулевой DoF) — на уровне опций. И даёт adversarial-стимул: чтобы узел исчез из анализа, достаточно **не сгенерировать** вариант его восстановления. Чем беднее набор кандидатов, тем больше узлов объявляются «безнадёжными» и выпадают.

Фикс (связка с `E`): обосновывать безнадёжность **достижимостью в мире/графе `G`**, а не отсутствием опции в текущем наборе; для исключения требовать машинно-проверяемое свидетельство `no_recovery` (отсутствие пути в `G`), а не «в кандидатах не нашлось». Три статуса из `E` (`reachable` / `proven_unreachable` / `unknown`) применяются здесь напрямую.

### 9.4 Остаток 2: §4.2 становится несущей конструкцией

При `ψ_var = V/(V + V_env)` (см. §10.1) ноль линзы возникает у **каждого** пассивного объекта. Значит масса сущностей приходит с `current_dof = 0` и зависит от правильности классификации §4.2. Ошибка даёт либо `−13.8` (ошибочно сочли оживляемой), либо молчаливое исчезновение из аудита (ошибочно исключили). Поэтому переход `lens-zero → entity-zero → exclusion` должен быть **явным и попадать в отчёт** (§6): иначе не видно, что именно камень «выпал».

## 10. Формула Variety (решено)

### 10.1 Формула и guard

```
ψ_var = 0,                 если V = 0
ψ_var = V / (V + V_env),   иначе
```

- `min(1, ·)` не нужен: `V/(V+V_env) ≤ 1` всегда.
- Guard `V = 0` перехватывает `0/0` **до** деления — поэтому отдельного случая `(0,0)` нет вовсе, есть просто ветка `V = 0`.
- Тело нормировки — **доля**, не порог (см. §10.3).

### 10.2 Определение `V_env` (решено)

`V_env` — **характеристика внешней среды**, а именно разнообразие **внешних возмущений** (Ashby-literal). Следствия:

- посылка `V ⊆ V_env` **не требуется**: тотальность обеспечивает сумма в знаменателе (не деление на `V_env`);
- **две роли среды** различаются: **возмущения** → `V_env` (знаменатель); **аффордансы** (инструменты, инфраструктура) → **`V`** (числитель) через достижимость;
- `V_env = 0` (тихая среда) при `V > 0` даёт `ψ = 1` — «вакуумная» устойчивость, **выводимая** из формулы, а не приписанная вручную.

Таблица случаев:

| `V` | `V_env` | `ψ_var` | Смысл |
|---|---|---|---|
| `> 0` | `> 0` | в `(0,1)` | обычный случай |
| `> 0` | `0` | `1` | среда спокойна ⇒ любого отклика достаточно |
| `0` | `> 0` | `0` | нет векторов отклика ⇒ коллапс (R1) |
| `0` | `0` | `0` | срабатывает ветка `V = 0` (guard) |

### 10.3 Семантика — принято: доля, а не порог

`V/(V+V_env)` — это **доля**, а не проверка неравенства Ashby:

| `V` относительно `V_env` | Формула | Ashby-порог |
|---|---|---|
| `V = V_env` (требование выполнено) | `0.5` | `1` |
| `V ≫ V_env` | `→ 1` (асимптотически) | `1` |
| `V = 0` | `0` | `0` |

То есть «требуемое разнообразие достигнуто» больше не читается как «полная свобода», и `ψ = 1` недостижимо. Это **осознанно принято** — плавная величина вместо признака «достаточно/недостаточно».

### 10.4 Унификация кернела

```
V/(V+V_env) = 1 / (1 + V_env/V)
```

Это **то же ядро `1/(1+x)`**, что предлагалось для Options-линзы (`ψ_opt = 1/(1+c/c₀)`, §2.2), только с `x = V_env/V` вместо `x = c/c₀`. Обе линзы описываются одним семейством профилей, и вопрос `A` сходится с вопросом `B`. Оговорка: для стоимости рекомендовалась экспонента `exp(−c/c₀)` ради аддитивности `ln ψ = −c/c₀`; единое ядро `1/(1+x)` этого лишает (`ln(1/(1+c/c₀))` не линеен). Компромисс «единообразие кернелов ↔ аддитивность штрафа за стоимость» решается в рамках `B`.

**Решено (§8.8, B-iv):** единое ядро **отброшено** в пользу аддитивности, потому что закон композиции обязателен, а ему удовлетворяет только экспонента. Unity линз обеспечивается не алгеброй, а **якорем по оси**: `V = V_env ⇒ ψ_var = 0.5` и `x_g = 0.5 ⇒ f_g = 0.5` — «половина ресурса ⇒ половина свободы». Комбинаторы при этом остаются разными по природе требования: возможности сравниваются с требованием (доля), требования удовлетворяются целиком (произведение по блокам).

### 10.5 Инструменты как фичи состояния (не сущности)

Инструмент (ключ, прибор, инфраструктура) **не моделируется как сущность со своим DoF** и не как ad-hoc «множитель». Он моделируется как **фича состояния/среды**, меняющая граф достижимости `D(S) = (V, E)`:

- «инструмент доступен» — новое различимое состояние (вершина, `V`);
- «воспользоваться инструментом» — доступное действие (ребро, `E`);
- эффект использования открывает **дополнительные рёбра** (новые возможности).

Вклад инструмента виден на обеих осях: state-DoF (`V`) и action-DoF (`ψ_opt`). Инфляция отсекается reachability-критерием (§4): «ключ в кармане» достижим → считается; «ключ на другой планете» → `Δ = 0`. Так инструмент работает **через линзы того, кому он доступен**, — без обнуления произведения и без вопроса об исключении «сущности-инструмента».

### 10.6 Оговорка про пассивные сущности

При произведении линз `ψ_var = 0` (а значит `current_dof = 0`) возникает у любой сущности без векторов отклика. Это разобрано в §9: судьба такого узла решается §4.2 (исключение известного ноля без пути восстановления), а не бесконечным штрафом. Требование к аудиту — видимость пути `lens-zero → entity-zero → exclusion` (§9.4). Отдельно отметим **два смысла `V = 0`**, различимых в отчёте: «отклики были и потеряны» (реальный коллапс) vs «откликов не было никогда» (пассивный объект).

## 11. Статус и путь к канонизации

Черновик, informative. Разделы §8–§10 добавлены и уточнены после обсуждения (вклад неизвестного, функция `u(t)`, бюджет времени и ворота действий §8.6, цена незнания §8.7, профиль стоимости §8.8, «безнадёжность» §4.2, формула Variety, инструменты как фичи состояния). По вопросу **A** принято: `V_env` = внешние возмущения (G), формула с guard'ом `V = 0`, шкала единая (R7), штраф за незнание — `u(t)` с привязкой к точке невозврата измерения `t*` (§8.5–§8.7), уровень `u₀` выбран — квантиль приора в защитной полосе `[0.251, 0.5]`, дефолт вырождается в H1 (§8.7). По вопросу **B** принято **полностью** (B-i…B-xii) — §8.8: закон композиции обязателен, экспонента, бюджет состояния как **мандат действующего агента**, якорь по блоку, конверсия ресурсов с **наблюдаемым** курсом, недоступность = неплатёжеспособность. По вопросу **C** принято: нормировка долей против внешнего зажима, `ψ_con = F/(F + F_env)` (§2.3). По вопросу **E** принято: безнадёжность обосновывается вердиктами по графу (`reachable` / `proven_unreachable` / `unknown`), а не отсутствием опции в наборе (§9.3, §8.9). По вопросу **H** принято (§8.7). По вопросу **I** принято: единица времени — микросекунды (`float64`), длительности берутся из именованной Perception-процедуры, ворота выполнимости универсальны (изъятие, а не штраф), дедлайн не покупается (§8.6). Нормативная часть внесена в `DOF-SPEC.md` (коммит `543d6b9`); правка досведена до согласованности (`v0.3`) — разбор в §11.1, остатки в §11.2. По вопросам **D** и **F** принято (§1.1, §2.4, §3, §8.2): произведение с обязательной разбивкой в отчёте и пер-линзовым `dof_known`; декларация — `psi = { id, digest }` в состоянии, полный текст в отчёте. **Открытых вопросов слоя измерения не осталось**, и слой канонизирован в `v0.4` (§11.3); все четыре порта реализуют `v0.4` и сходятся по дайджесту (§11.2).

**Собранные находки по нормативному тексту** (`SKILL.md` / `DOF-SPEC.md`; статус каждой — при самой находке и в §11.1):

1. §4.1 не определяет числовой вклад «неизвестного», хотя §4.2 на него ссылается — пропуск делает запрет на исключение неизвестных символическим (см. §8). *Закрыто в `v0.4`: вклад определён в §4.7 (терм-уровень, `u(t)`, пер-линзовый `dof_known`), реализовано во всех четырёх портах.*
2. Недоопределён главный интерфейс: три линзы toolkit'а имеют несовместимые единицы, и нигде не сказано, откуда берётся `current_dof` (§0). *Закрыто в `v0.4`: §4.1 (произведение), §4.6 (линзы, guard), §6.1 (разбивка), §3.4 (декларация) — нормативны и реализованы; сам toolkit переписан под §4.6 (см. §11.2).*
3. §4.3 определяет `NetDelta_index`, §4.4 вычитает из `NetDelta` — расхождение символов (§8.8).
4. `−0.5` за необратимость — плоский нат в сумме по сущностям, относительный вес зависит от `|calc|` (§8.8).
5. §4.4 требует ровно `0.5`, что численно совпадает с оценкой незнания в H1 и создаёт коллизию смыслов (§8.7, §8.8).
6. Правка по пункту **I** (коммит `543d6b9`) была не согласована сама с собой. **Исправлено в `v0.3`:** семь расхождений и то, чем каждое закрыто, — §11.1.
7. **`§8.9` не существует, а на него ссылались три нормативных места.** §3.4.1 (состав декларации: горизонт восстановления `T_rec(X)`, класс допустимых средств `M(S)`, вердикты достижимости), §4.2 (клауза включения в `calc`: «recoverable in principle») и §4.5 (первая ступень лексикографической лестницы). В SPEC нет вовсе никакого §8.9: §8 «Wire / Serialization Contract» не имеет подразделов и заканчивается примером JSON. То есть **нормативное правило включения в расчёт опиралось на несуществующую норму**: «recoverable in principle» можно было трактовать как угодно, и §7 этого не поймал бы — две реализации разошлись бы ровно на сущностях с `current_dof = 0`, то есть там, где исключение и происходит. **Закрыто без новой математики:** мёртвые ссылки убраны, в §4.2 стоит явная пометка **reserved** с прямым указанием, что делать реализациям до появления определения (сущность с `current_dof = 0` и `dof_known == true` исключается — так и делают все четыре порта), а §3.4.1 и §4.5 помечены как недействующие до определения. Правило, которое при этом нарушалось: **основные документы не ссылаются на `drafts/`** — в README всех шести языков написано, что черновики «may contradict the current standard and must not be cited as normative». Первое моё «закрытие» было ссылкой из SPEC на черновик, то есть тем же нарушением в новой форме; сделано заново без внешних ссылок. Само определение (горизонт, класс средств, три вердикта) — работа версионного релиза, §11.2.
8. **Правило включения в `calc` сформулировано дважды и по-разному.** `SKILL.md` (слой намерения): «включён, если `current_dof == 0` **и хотя бы одна доступная опция может поднять его DoF**» — критерий зависит от **набора кандидатов**; и в этой формулировке нет клаузы про `dof_known`, то есть сущность с неизвестным DoF при нулевом `current_dof` по букве философского слоя исключается, тогда как `DOF-SPEC` §4.2 запрещает её исключать. Расхождение принципиальное: генераторно-зависимый критерий даёт **стимул держать набор вариантов бедным** — чем меньше кандидатов, тем больше узлов объявлены безнадёжными и выпадают из анализа (нарушение Axiom 7 «не снижать DoF из-за отсутствия структуры» и ровно то, что отвергнуто по вопросу **E**, §9.3). **Закрыто частично:** в §4.2 добавлен явный запрет — свидетельством невосстановимости **не может** быть текущий набор опций генератора. Полное закрытие — за версионным релизом, который определит достижимость по графу мира `G` и приведёт `SKILL.md` к той же формулировке.
9. **Индекс вознаграждал ликвидацию.** §4.2 исключал сущность с известным нулём, а её член `≤ 0` при этом **исчезал из суммы** ⇒ `ΔIndex` от убийства был **положительным** (до `+13.8`), а ликвидация самого срочного узла вдобавок отодвигала `τ`. Черновик §8.9.3 утверждал обратное («the victim remains in the count as a −13.8 penalty»), и это верно **только** при действующей P-rule. Хуже: порты реализовали **генераторно-зависимую** клаузу («держим известный ноль, если какая-нибудь опция его оживляет»), то есть цена убийства зависела от того, догадался ли генератор предложить восстановление. **Закрыто в `v0.5`:** набор членов замораживается по `S` (плата за коллапс), генераторно-зависимая клауза удалена из всех четырёх портов (§11.4).
10. **Axiom 3 не имел механизма.** Сумма логов компенсирующая; ε-пол делал коллапс **конечным** (`≈ −13.8`), то есть «no finite gain can offset a collapse» было неправдой; лексикографический фильтр **T1** не существует ни в `SKILL.md`, ни в SPEC, ни в `references/`. Черновик это признавал (пример 3 в §6: `ΔIndex = +0.21` при `Humanity 0.8 → 0.05`), но записано это не было нигде. **Закрыто частично в `v0.5`:** структурный фильтр (§4.5, разрушающая опция изымается, пока есть безвредная), плата за коллапс (§4.2) и действующая первая ступень лестницы («наименьшее число разрушений») делают запрет исполняемым. Полная лексикографическая защита (T1) остаётся открытой — §11.2.
11. **Ярлык `is_collapse_source` без свидетеля.** `SKILL.md` определяет Collapse Source через «…**without structural utility**»; SPEC просто читает булев флаг, а §6 не требует обоснования. Значит сущность с флагом `true` выпадает из индекса и из `τ`, не заплатив ничего, а определение в `SKILL.md` строго уже правила. **Открыто:** в `v0.5` закрыта только расчётная часть (цену платит тот, кто разрушил), а требование к ярлыку — машинно-проверяемый «акт коллапса» (§8.9.3) — относится к версионному релизу достижимости.
12. **`ΔIndex > 0` и базовая линия «остаться» потеряны.** `SKILL.md` (Decision Calculus 4): «a new path is selected only if its projected gain strictly exceeds … `> 0`» — то есть «ничего не делать» это референс с `NetDelta = 0`. SPEC §4.5 выбирал `argmax` без порога и без варианта «остаться» в наборе ⇒ когда все варианты хуже покоя, система действовала бы по наименее плохому. **Закрыто в `v0.5`.**
13. **«−∞» против ε-пола.** Действующее соглашение: на уровне стандарта пишется `−∞`, а числовой пол — сноска «Implementation note (numerics only)» в §4.1 и код. Но утверждение «no finite gain can offset a collapse» (`SKILL.md` §3) и «Blocks any path with a `−∞` penalty» (`patterns/PATTERNS.*.md`) при конечном поле были **ложны**: пример 3 из §6 показывает, что конечный выигрыш перекрывает пол. **Закрыто:** §4.1 прямо указывает, что защита структурная (§4.2/§4.5), а `PATTERNS.md`/`PATTERNS.fr.md` приведены к тому же смыслу. **Уточнение 2026-09-16:** правка тогда дошла только до этих двух файлов. Проверено грепом перед проходом `v0.6`: в `PATTERNS.ru/de/es/eo.md` формулировка «blocks any path with a `−∞` penalty» и секундный порог `τ < 5` жили **ещё две ревизии** — то есть запись «приведены» была верна для двух файлов из шести. Все шесть приведены к смыслу §4.1/§4.2/§4.5 и к нормативным микросекундам §5, заодно с добавлениями `v0.6` (ресурсный ворот, средства агента, производные блоки).
14. **`references/framing-traps.md` §4 противоречил аксиомам.** Там было «agency is a resource» и «focus on the Sum of DoF», то есть утилитарно-компенсационное прочтение, которое Axiom 3 запрещает; при этом файл обязателен к применению по `SKILL.md` (Decision Calculus 1). **Закрыто:** раздел переписан — запрещён именно *внешний* счёт, а структурная защита объявлена частью исчисления; агентность больше не «ресурс».
15. **PoI был оформлен как условие лицензии.** `references/license.md` объявлял Proof of Implementation «a condition of using the DOF-Core skill» и сокрытие расчётов — нарушением «духа ShareAlike». CC BY-SA регулирует авторские права, а не поведение исполняемой системы; условие PoI — отдельный ковенант. **Закрыто:** PoI переписан как условие *заявления о совместимости*, без ограничения прав, данных лицензией.
16. **Числа профиля B не соответствовали нормативной формуле.** §8.8 принимает B2 как `exp(−c/c₀)` (основание `e`), а канон — `4^(−x_g)` с якорем `x_g = 0.5 ⇒ 0.5`; таблицы и порог «ухода под пол» (`13.8·c₀` против `≈10·C_g`) у двух форм разные. **Закрыто:** в §8.8 добавлена оговорка о двух формах; нормативной объявлена каноническая.

**Требуемые добавки к модели** (уровень SPEC, вне черновика; нужны также пункту **I**):

| Добавка | Зачем | Без неё |
|---|---|---|
| **вектор бюджета в `State`** | многоступенчатое прогнозирование и накопление трат | бюджет остаётся масштабным референсом, переиспользуемым каждый цикл (§8.8, B-ix) |
| **`projected_budget_delta` у `ActionOption`** | симметрия с `projected_dof_delta`: ресурс, который действительно тратится, обязан иметь проектируемое изменение | стоимость влияет на оценку, но не уменьшает бюджет |
| **именованная Perception-процедура структуры обмена** (разбиение на блоки + курсы как наблюдаемые цены) | курс берётся из мира, а не объявляется; идентификация по `psi_id` (F) | курс остаётся свободным параметром, подделываемым как угодно (§8.8, B-xi) |
| **пер-линзовая разбивка в отчёте §6** (три терма на сущность + `dof_known` каждой линзы + связывающая линза) | сверка реализаций становится построчной: погашающие друг друга ошибки видны, а не растворяются в итоге (§3, §8.2) | §7 проверяется только по итоговому числу, и две взаимно компенсирующиеся ошибки проходят как совпадение |
| **поля отчёта об изъятых опциях** (список `option_id` + какие ворота сработали) | ворота §8.6 становятся проверяемыми: изъятие — решение, а не молчаливый пропуск | изъятие опции невидимо в аудите — тот же класс, что молчаливое исключение сущности (§11.1) |
| **`psi = { id, digest }` в `SystemStateMatrix`** | R7 становится проверяемым: шкала заморожена как вход, а подгонка знаменателя обнаруживается хешем (§1.1) | «заморозка» остаётся обещанием, и вариант может улучшить оценку подгонкой знаменателя |

Пока этих добавок нет, «доступность» относится **только к текущему циклу**, и это должно быть написано в SPEC прямо.

### 11.1 Правка SPEC по пункту I: расхождения и как они закрыты

Правка по I (коммит `543d6b9`) была сделана неаддитивно и примерно на треть — следствие сбоя в процессе. Ниже семь расхождений и результат: шесть закрыты в `DOF-SPEC.md` `v0.3` и в портах, седьмое остаётся пробелом.

1. **Потеря поля `is_reversible`.** `estimated_duration_mks` встала **на место** строки `is_reversible`, а не рядом, хотя §4.4 её использует (`NetDelta -= 0.5`) и §6.1 перечисляет в аудите. **Закрыто:** в таблице §3.3 обе строки, происшествие записано в §10.
2. **Переименование выполнено частично.** На μs были переведены три строки таблиц и §5 — пример-фикстура при этом читалась как `τ = 4` **микросекунды**, то есть давала бы другой вердикт. **Закрыто:** все упоминания переведены (`global_time_to_collapse_mks`, `time_to_collapse_mks`), пример §8 пересчитан (100 s → `100000000.0`, 4 s → `4000000.0`), §6.2, §7, §9, §10 — в μs. Вердикт примера сохранён: `4 < 5` секунд ⇔ `4·10⁶ < 5·10⁶` μs — FAST_PASS.
3. **Порты не обновлены.** После смены единицы они молча считали в `10⁶` раз другое. **Закрыто:** `patterns/python`, `patterns/cpp`, `patterns/go`, `patterns/rust` переведены на μs, получили поле `estimated_duration_mks` и ворота выполнимости §5. Все четыре собраны и запущены; результат совпадает посимвольно: `total_system_dof = −3.2188758248682006`, `net_delta = 1.5594379124341002`.
4. **Дублированный абзац** *Viability Gate*. **Закрыто:** дубликат удалён.
5. **Сломанные строки таблиц** (`||` перед именем поля). **Закрыто:** три строки исправлены.
6. **Тип `float64` против `float`.** **Закрыто:** новые поля приведены к стилю таблиц, а в §3 добавлена конвенция — `float` означает IEEE-754 binary64, даункаст в single запрещён (иначе §7 о бит-эквивалентности невыполним).
7. **Изъятия не попадают в отчёт.** §6.2 не сообщает, какие опции изъяты воротами и по какой причине. **Открыто** — это пробел, а не ошибка правки: имена полей надо выбрать (см. §11.2).

Попутно закрыто ещё три расхождения, найденных при разборе:

- **`1e9` как «safe large value»** для τ: в секундах это ≈31.7 года, в μs — 16.7 минуты, то есть другой смысл. Заменено на `1e15` μs (те же ≈31.7 года) в SPEC и во всех портах.
- **Расхождение символов `NetDelta_index` (§4.3) против `NetDelta` (§4.4)**, из-за которого было неясно, когда применяется штраф за необратимость. Сведено к `NetDelta`, порядок применения штрафов записан явно.
- **Go-порт печатал JSON в CamelCase** (`GlobalTimeToCollapseMks`), нарушая проволочный контракт §8. Добавлены теги `json:"…"` с именами полей §3; вывод приведён к тому же виду, что у python-порта.

Версия поднята `v0.2 → v0.3`: по §10 изменение нормативной константы (`FAST_PASS_THRESHOLD`) — версионное событие, а не молчаливая правка.

### 11.2 Что осталось

- **Слой измерения реализован во всех четырёх портах.** `patterns/python` (42 проверки), `patterns/go` (40), `patterns/cpp` (39), `patterns/rust` (39) считают линзы, произведение, `u(t)`, пер-линзовые термы, `binding_lens`, `floored`, дайджест декларации, ворота с записью изъятий и правила `v0.5` (замороженный `calc`, плата за коллапс, структурный фильтр, базовая линия «остаться», покрытие неизвестного, `incomplete`). На общей фикстуре все четыре дают одни и те же числа и **один и тот же дайджест декларации** (`e6f58a7e9dc0ac58…`, SHA-256) — кросс-языковая канонизация подтверждена исполнением, а не только требованием в тексте. Контрпример §6 (необратимый процесс: произведение против суммы) исполняется в python-порте.
- **Именованных процедур Perception всё ещё нет.** Порты принимают сырые счётчики (`V`, `V_env`, блоки `(c_g, C_g)`, `F`, `F_env`) и считают линзы сами, но сами процедуры существуют только строкой в декларации (`procedures`), а не как отдельные компоненты: числа по-прежнему приходят извне.
- **Каноническая форма декларации зафиксирована в §3.4.3** (UTF-8 JSON, отсортированные ключи, без незначащих пробелов, счётчики целыми, дроби — строкой с шестью знаками `%.6f`, без экспоненты) и **подтверждена кросс-языково**: python, C++, Go и Rust дают бит-идентичный дайджест SHA-256 на одной и той же декларации.
- **Совпадение Fast Pass и `t* ≤ 0` опровергнуто, оба условия записаны.** Это разные величины: режим — порог по `τ`, окно измерения — `τ − T_meas`. Контрпримеры в обе стороны: `τ = 10 s`, `T_meas = 12 s` — DEEP-режим без окна измерения; `τ = 4 s`, `T_meas = 1 s` — FAST PASS с открытым окном. В §5 записано прямо: тождества нет, условия вычисляются независимо, выводить одно из другого запрещено. Там же закрыт граничный дефект: гейт изымал опцию только при `d > τ`, а §4.7 объявлял окно отсутствующим уже при `t* ≤ 0` — теперь для опции-измерения требуется строгое `T_meas < τ` (измерение, завершающееся ровно в момент коллапса, бесполезно), а для обычного действия правило прежнее (`≤ τ`). Порты опций-измерений не моделируют, поэтому их поведение не изменилось.
- **Остаток B — в работе (`v0.6`).** Перенесён по протоколу §11.5: средства агента по ресурсам у состояния, массив расхода у опции, ворота неплатёжеспособности, производные блочные числа, единицы и курсы в декларации. Имя поля — `projected_resource_delta` (не `projected_budget_delta`): первичны ресурсы, а группы считает анализ.
- **Прозаические тексты приведены к единицам.** Закрыто: `patterns/PATTERNS.md`, `patterns/PATTERNS.fr.md` и шесть README говорят «τ < 5 000 000 µs (5 s)», а не «5 s»; в SPEC §5 порог и так нормативен в микросекундах. Прозаическим остаётся только сам черновик (там секунды фигурируют как пояснение к константе). **Уточнение 2026-09-16:** здесь та же неполнота, что и в №13 — микросекунды тогда появились только в `PATTERNS.md` и `PATTERNS.fr.md`, а `PATTERNS.ru/de/es/eo.md` продолжали говорить «$\tau \ge 5$ сек / Sekunden / segundos / sekundoj». Исправлено во всех шести (плюс норма «секунды в скобках» сохранена как пояснение, а не как величина).
- **Полная не-компенсация (T1) не введена.** `v0.5` сделал исполняемыми запрет на разрушение счётного узла (плата за коллапс + структурный фильтр + первая ступень лестницы), но лексикографического фильтра допустимости в целом — запрета «компенсировать» провал одной линзы ростом другой, запрета конечным выигрышем перекрывать любой провал — в стандарте нет. Пока его нет, Axiom 3 исполнен ровно в той части, которую стандарт умеет проверить машинно.
- **Ярлык `is_collapse_source` требует свидетеля.** В `v0.5` закрыта расчётная часть (цену платит тот, кто разрушил), но сам ярлык по-прежнему выставляется без доказательства «акта коллапса», хотя `SKILL.md` требует «without structural utility», а §8.9.3 черновика — машинно-проверяемого акта. Это работа версионного релиза достижимости.
- **Смоук-тесты покрывают и DEEP-режим.** Закрыто: в каждом из четырёх портов есть сценарий с большим `τ` (100 s) и проверка `mode = DEEP_DIVERSIFICATION` рядом с FAST_PASS-веткой (у ребёнка дедлайн 4 s при пороге 5 s).
- **Аномалия инструментальной цепочки, не спецификации.** `rustc 1.95.0` в этой песочнице при `-C opt-level ≥ 1` даёт неверный режим, если измеренное состояние передавать в решающую функцию **по значению**: сравнение `τ` читает устаревшее значение (режим и ворота приходят как из предыдущего вызова), при `opt-level=0` всё верно, а `-C opt-level=1`/`2`/`3` воспроизводимо ломаются. Go, C++ и Python на полной оптимизации верны. Порты и тесты от этого не зависят: в `patterns/rust/orchestrator.rs` состояние передаётся по ссылке (`decide(&state)`) с комментарием о причине, проверка зелёная и на `opt-level=2`, и на `0`.
- **`references/dof-assessment-toolkit.md` приведён под стандарт.** Закрыто: справочник больше не вводит собственных формул — он описывает **съём счётчиков** (`V`, `V_env` с горизонтом, блоки `(c_g, C_g)` и «дорого ≠ невозможно», `F`, `F_env`, длительности), ссылается на §4.1/§4.6/§4.7, в шапке объявляет приоритет SPEC и **на `drafts/` больше не ссылается**. Снято то, что прямо ломало нормативную математику: «DoF — количество векторов реакции» (давало `+1.95` нáта в неположительной сумме), «DoF — обратная величина стоимости» (неограничена вверх), `DoF = переменные − ограничения` (уходит в минус, `ln` не определён) и Integration Note, подсказывавший подставлять счётчики прямо в `Σ ln(DoF)`. Добавлены: запрет писать `0` вместо недостижимого счётчика (§4.7), правило различия аффордансов и возмущений (ключ — не враг), объяснение «доля вместо порога» для неравенства Эшби (`V = V_env` даёт `0.5`, а не «всё хорошо»), таблица «что записать по сущности» под §3.4 и числовой пример (`0.5 · 0.870551 · 0.75 = 0.326456`, термы суммируются в `ln` произведения). Формулы-эхо искали по всему репозиторию — в README шести языков и `PATTERNS.*` их нет, ссылки на файл остались корректными. **Дополнение 2026-09-16 (под `v0.6`):** справочник был приведён и к ресурсному слою — список снимаемых счётчиков больше не содержит блоки `(c_g, C_g)` как входные данные (§4.6 объявил их производными: снимать надо требования по ресурсам, средства агента, выведенные группы и наблюдаемые курсы); добавлены «дорого ≠ невозможно» на ресурсной стороне с вердиктом неплатёжеспособности, правило «неизвестен собственный остаток — недопустимый вход», запись единиц/групп/курсов/мандата один раз на состояние и списание времени обмена в тот же `τ`. Раздел «See also» дополнен §3.2, §3.3, §4.8 и указанием на `conversion_applied`.
- **Достижимость (`T_rec`, `M(S)`, вердикты) не канонизирована.** В SPEC она помечена **reserved**: сущность, приходящая в состояние уже на известном нуле, исключается. При этом защита от ликвидации счётного узла теперь **не зависит** от этого определения (плата за коллапс и структурный фильтр, §11.4), и полное определение остаётся работой версионного релиза: горизонт восстановления, класс допустимых средств, три вердикта, свидетельство `no_recovery` плюс граф `G` во всех четырёх портах.

### 11.3 Канонизация слоя измерения (`v0.4`)

Слой измерения перенесён в нормативный текст добавлением, без перенумерации существующих разделов `DOF-SPEC`:

| Куда | Что внесено |
|---|---|
| §3.2, §3.4 | поле `psi = { id, digest }` в `SystemStateMatrix`; закрытый перечень содержимого декларации, разложение по уровням, требование канонической сериализации, правило «разные дайджесты не сравниваются» |
| §4.1 | `DoF(e)` определён как произведение трёх линз; прямое значение `current_dof` допустимо, только если равно этому произведению |
| §4.6 | три линзы и их нормировка (`ψ_var`, `ψ_opt`, `ψ_con`); единый guard вырожденного случая; доводы за произведение и против минимума; якоря |
| §4.7 | терм-уровень; пер-линзовый `dof_known`; штраф за незнание `u(t)` с точкой невозврата `t*` и полосой `U_MIN`/`U_MAX`; нормированность и свойства |
| §5 | informative-оговорка про ворота ценности информации (нормативного алгоритма нет) |
| §6.1, §6.2, §6.3 | пер-линзовые термы и связывающая линза; `psi_id`, `psi_digest`, текст декларации; список изъятых опций; длительность опции в отчёте |
| §7 | пункты 7–9 (линзы, дайджест, ворота) и требование терм-уровневой эквивалентности вместо сверки одного итога |
| §10 | версия `v0.4`, запись изменений, расширенный список нормативных констант |

**Развилки, решённые при переносе** (все три — на моё усмотрение, по твоему указанию):

1. **Изъятые опции в отчёте** — внесены как `removed_options: [{option_id, gate}]` (`gate = "viability"`). Ворота §5 нормативны, значит отчёт обязан их показывать; молчаливое изъятие было бы того же класса, что молчаливое исключение сущности.
2. **Ворота ценности информации** — только informative-оговорка в §5: нормативным может быть лишь то, что имеет процедуру, а процедуры нет. Конформность от их реализации не зависит.
3. **Проекция `τ`** — нового поля не вводится. §4.3 уже требует `S'.global_time_to_collapse_mks = S.global_time_to_collapse_mks`, поэтому `τ` жёсткий внутри цикла, а отсрочка приходит **новым наблюдением** от Perception. Фраза в §8.6 приведена к этому же.

**Что это закрывает.** Находки №1 и №2 из §11 (недоопределённый вклад «неизвестного» и недоопределённый интерфейс `current_dof`) больше не дефекты нормативного текста: вклад определён в §4.7, интерфейс — в §4.1 и §4.6. Исполнение доведено до всех четырёх портов (30 проверок python, 28 Go, 27 C++, 27 Rust — все зелёные, на общей фикстуре; python, Go и C++ прогнаны на полной оптимизации, Rust — на `-C opt-level=2` и `0`): включая проверку того, что `current_dof` равен произведению линз, что пер-линзовые термы суммируются в вклад сущности (кроме случая сработавшего ε-пола — тогда отчёт помечает `floored`), что пассивный объект даёт `ψ = 0` без NaN и без бесконечного штрафа, что неизмеренная линза стоит `ln u₀` и не исключается, что дайджест стабилен и меняется при смене счётчика, что ворота §5 изымают опцию и это видно в отчёте, и что произведение обваливает индекс там, где сумма замаскировала бы опасность. Сверх того: все четыре порта выдают **один и тот же дайджест декларации** на одной и той же фикстуре (`e6f58a7e9dc0ac58…`), то есть канонизация §3.4.3 выполнима одновременно в Python, C++, Go и Rust.

Слой измерения канонизирован в `v0.4` (§11.3); открытых вопросов **A–I** не осталось.

### 11.4 Структурная безопасность (`v0.5`)

Аудит окончательных решений черновика против `SKILL.md`, `DOF-SPEC.md` и `references/` показал, что ядро в большинстве расхождений не ошибается — ошибалась **реализация**: нормативный текст не обеспечивал того, что обещают аксиомы. Внесённое:

| Находка | Где закрыто |
|---|---|
| №9 Индекс вознаграждал ликвидацию | `§4.2`: **замороженный `calc`** — набор членов считается по `S` и используется для каждого `S'`; сущность, доведённая до известного нуля, остаётся в сумме и платит пол `ln ε`. Плата видна построчно (`collapse_charges`, `§6.3`) |
| №12 Потеряна базовая линия «остаться» | `§4.5`: опция выбирается только при `NetDelta > 0`; `NetDelta = 0` — это «ничего не делать» по определению, иначе `none` и запись в отчёте |
| №10 У Axiom 3 не было механизма | `§4.5`: **структурная допустимость** — опция с непустой платой изымается, пока есть безвредная (`gate = "collapse"`); первая ступень лестницы — «наименьшее число разрушений» |
| №13 «−∞» против конечного пола | `§4.1`: прямо сказано, что защита структурная (`§4.2`/`§4.5`), а пол — числовая уступка; `patterns/PATTERNS.*` и шесть README приведены к тому же смыслу. Проверено 2026-09-16: правка 2026-09-15 дошла только до `PATTERNS.md`/`PATTERNS.fr.md`, остальные четыре файла несли старое утверждение две ревизии; приведены все шесть |
| №14 `framing-traps` §4 противоречил аксиомам | `references/framing-traps.md`: запрещён *внешний* счёт, структурная защита объявлена частью исчисления, агентность перестала быть «ресурсом» |
| №15 PoI как условие лицензии | `references/license.md`: PoI — отдельный ковенант и условие *заявления о совместимости*, права CC BY-SA не сужаются |
| №16 Числа профиля B | черновик §8.8: оговорка о двух формах (`e` — в рассуждении о законе композиции, `4^(−x)` — норма) |
| Состав декларации ≠ хешируемый набор | `§3.4.1` разделён на *hashed content* (закрытый список, совпадает с тем, что реально хешируют порты) и *report context* |
| `V_env` нигде не определён | `§4.6`: `V_env` — внешние возмущения; аффордансы входят в `V` через достижимость и в `V_env` не считаются |
| Коллизия значения `0.5` | `§4.4`: коэффициент жёсткости явно разведён с `U_MAX`; перенос жёсткости в стоимость — reserved |
| Неизвестное могло стать невидимым | `§4.7`: покрытие (`projected_dof_delta` обязан назвать каждую сущность с `dof_known = false`) и флаг `incomplete` в отчёте |

Проверка: четыре порта прогнаны заново на общей фикстуре — Python 42 проверки, Go 40, C++ 39, Rust 39 (плюс сборка `opt-level=0`), все зелёные. Числа совпадают между языками: плата за коллапс `Δ = −12.9429` нáта, действие по пассивному объекту меняет индекс ровно на `0`, структурный фильтр помечает изъятие `gate = "collapse"`, набор из одних убыточных вариантов не выбирает ничего, дайджест декларации неизменен (`e6f58a7e…`) — то есть `v0.5` не тронула линейку, только правила.

Сознательный предел шага: исполнена та часть Axiom 3, которую стандарт умеет проверить машинно (разрушение счётного узла не окупается и не допускается при наличии безвредной альтернативы). Полная не-компенсация (T1), определение достижимости и свидетельство «акта коллапса» для ярлыка агрессора остаются версионным релизом.

Единственная правка вне SPEC и `references/`: в `SKILL.md` уточнены два пункта **раздела Definitions** (не аксиомы) — определение DoF приведено к нормированной шкале `[0,1]`, а «DoF = 0» перестало означать «всегда необратимое уничтожение», потому что пассивный объект тоже даёт нулевую линзу и различие между ним и коллапсом несёт расчёт, а не определение. Раздел Core Axioms не тронут.

**Остаток целиком** (§11.2): именованные процедуры Perception, полная не-компенсация (T1), канонизация достижимости (`T_rec`, `M(S)`, три вердикта) вместе со свидетельством «акта коллапса», совпадение «Fast Pass ⇔ `t* ≤ 0`», `projected_budget_delta` (остаток от B) и контрпримеры §6 как фикстуры SDK.

### 11.5 Решения по бюджету (`v0.6`)

Протокол обсуждения (16.09): что здесь первично, в каких единицах считается расход и как считается нехватка. Нормативные формулировки пишутся уже от этого раздела.

**1. Первичны лимитированные ресурсы.** У каждого ресурса объявляется единица: **имя** (`J`, `kWh`, `s`, `machine-hour`, `RUB`) и **масштаб** (коэффициент к базовой единице класса; для денег — валюта). Действие объявляет **массив расхода по ресурсам в физических единицах**. Обязательный минимум — **время и энергия**; нулевой расход объявляется явно (`energy: 0.0`), а не опускается — то же правило, что для неизвестного в §4.7: отсутствие ключа неотличимо от «не подумали».

**2. Групп на уровне действия нет.** Группы взаимно обмениваемых ресурсов — принадлежность **анализа**, а не описания действия. Группа выводится из **наблюдаемых** путей обмена (как и курс, B-xi) и вносится в декларацию уже выведенной: тогда группа не может стать способом спрятать нехватку, а воспроизводимость сохраняется.

**3. Время — две роли и два поля.** `estimated_duration_mks` — длительность против `τ` (ворота §5; **не покупается**), и «рабочее время / машино-часы» — обычный ресурс в массиве расхода, покупаемый по курсу. Сливать их нельзя: иначе однажды не заметишь, что обмен купил время, которого в дедлайне нет.

**4. Конверсия — проверяемая операция, а не подстановка.** Она проходит **те же ворота**, что действие, включая время обмена (оплата, постановка задачи); условия допустимости — путь обмена, подходящее предложение, наличие средств, работающий канал платежа, время поставки. Три исхода для действия: (а) ресурсов хватает напрямую — конверсия не нужна вовсе; (б) не хватает, но дефицит покрывается обменом внутри группы — требование = прямое + конвертированный дефицит; (в) обмен невозможен, не успевает или сам не по средствам — **ворота**: вариант изымается, а не штрафуется.

**5. `τ` — единственный неконвертируемый ресурс.** Отсрочку даёт только действие, меняющее сам `τ`.

**6. Неизвестен свой остаток по ресурсу — недопустимый вход**, а не режим оценки (B-xii): свой бюджет самоизмерим, поэтому «не знаю» означает «замерь, иначе решение неполно». Симметрии с `u(t)` здесь нет намеренно: неизвестность мира оценивается штрафом за незнание, неизвестность собственных средств — запретом решать. Относится ко всем ресурсам, а не только к обязательному минимуму.

**7. Что в декларацию, что в отчёт.** *Hashed content:* идентификаторы ресурсов, единицы и масштабы, средства действующего агента, состав групп, наблюдаемые курсы, объявленный мандат. *Report context:* происхождение курса (наблюдённая цена и имя процедуры), что именно конвертировалось и по какой цене, остаток средств до и после.

**8. Блочные числа — производные.** `(c_g, C_g)` в линзе вычисляются именованной процедурой из сырых входов (требования по ресурсам + средства + группы + курсы), и **выведенные числа обязаны совпадать с тем, что считает процедура** по тем же сырым входам. Иначе две реализации с одинаковыми `(c_g, C_g)`, но разными группами или курсами дадут один дайджест и разные отчёты.

**9. Обратимость из `v0.6` исключена.** Она не ресурс: ни единицы, ни лимита у неё нет, и платить за неё из бюджета не из чего. Плоские `−0.5` натов в §4.4 остаются как временная величина с записанным размерностным дефектом; правильный дом — DoF-пространство (`v0.7`): необратимое действие **закрывает переходы**, и линза видит это как сужение набора, а не как плату.

**Следствия для модели (заготовка `v0.6`).** Стоимость агент-относительна, DoF сущность-относителен (B-viii) — отсюда форма: расход атрибутируется сущности, списывается с агента.

```
ActionOption:
  projected_resource_delta: { entity_id → { resource_id → delta } }   # ±: минус — расход, плюс — прирост
  estimated_duration_mks:   float                                     # время против τ (обязательно, уже есть)
SystemStateMatrix:
  resources: { resource_id → available }                              # средства действующего агента
Declaration (hashed content):
  resources: [ {id, unit, scale} ]  groups: [ {id, members} ]  rates: {…}  mandate: {…}
```

| Уровень | Что меняется |
|---|---|
| §3.1 | у состояния появляются **средства действующего агента по ресурсам** вместо расплывчатого «бюджета» |
| §3.3 | у опции — массив расхода по ресурсам (энергия обязательна, время — существующее поле длительности, ноль явный) |
| §4 | **третьи ворота: неплатёжеспособность.** Прямое покомпонентное сравнение → проверенная конверсия → если и после неё не по средствам, опция изымается (`gate: "insolvency"`) |
| §4.6 | числитель и знаменатель линзы объявлены **производными** от сырых входов |
| §3.4.1 | расширение закрытого списка: ресурсы, единицы и масштабы, средства, группы, курсы, мандат |
| §6 | в строке опции — объявленный расход, применённая конверсия, запись ворот; в отчёте по состоянию — остаток до и после |
| §5, §4.7 | прямые оговорки: `τ` не ресурс; неизвестный свой остаток — недопустимый вход |
| §4.4 | **без изменений**: плоские `−0.5` с пометкой о дефекте, закрытие в `v0.7` |

**Фикстуры для доказательства.** (1) Две опции с одинаковым эффектом и разным расходом — выигрывает дешёвая, разрыв `NetDelta` равен разнице цен (`ln 4` на единицу `x`). (2) Расход выше средств после полной торговли — изъятие с `gate: "insolvency"`, видимое в отчёте. (3) Сложный план из двух шагов — расход по ресурсу суммируется (B-iv), требование одно. (4) Нехватка покрыта обменом по наблюдаемому курсу — числа как в примере §8.8 (`x_g = 0.6`, строка `−0.832` ната). (5) Разные единицы или масштабы при одном имени ресурса — **разный дайджест** (доказывает, что единицы действительно в линейке). (6) Кросс-языково: одинаковый дайджест и одинаковые числа во всех четырёх портах; эталон пересчитывается.

### 11.6 Реализация `v0.6`: что именно закреплено

Ниже — решения, принятые при переносе §11.5 в код и текст. Все они — уровня реализации и нормативного текста; перечисляю их отдельно, потому что каждое пришлось выбрать, а не вывести.

**Формы полей (как построено).**

```
SystemStateMatrix:  resources: { resource_id → available }
ActionOption:       projected_resource_delta: { entity_id → { resource_id → delta } }
Declaration (hashed): resources: [ {id, unit, scale} ]   # отсортированы по id
                      groups:    [ [resource_id, …] ]    # канонизированы
                      rates:     { "from->to" → { rate, duration_mks } }
                      mandate:   { … }
                      procedures: + "options_blocks": "<psi_id>:derive_blocks"
LensObservation:    requirements: { resource_id → required }   # вместо авторских options
```

**Ключ курса несёт направление.** `"credit->energy"` означает «за одну единицу `credit` дают `rate` единиц `energy`»; в курс положена и **собственная длительность обмена** (`duration_mks`). Направление читается из ключа, а не угадывается по величине, и две реализации не могут разойтись в том, что на что меняется.

**Ресурс вне объявленных групп становится одиночной группой.** `canonical_groups` нормализует объявленные группы (члены отсортированы, список групп отсортирован) и добавляет по одной группе на каждый ресурс, который встречается в требованиях или средствах, но ни в одну группу не попал. Иначе требование могло бы **молча выпасть из вывода** — тот же класс дефекта, что неизвестный без покрытия в §4.7.

**`derive_blocks` — тотальная функция.** Для каждой группы: `c_g = Σ max(0, requirement_r)`, `C_g = Σ max(0, means_r)`. Ноль — законная величина на обеих сторонах: блок с `c_g = 0` линза пропускает, блок с `c_g > 0 ∧ C_g = 0` даёт ноль по воротам §4.6. Вывод не может «не получиться»: каждый ресурс входов лежит ровно в одной группе.

**Кассовый журнал важнее объявленного расхода.** Сначала тратятся прямые средства по ресурсу, затем дефицит покупается, и **платит тот ресурс, которым заплатили**, а не тот, что объявлен в расходе: опция, израсходовавшая 12 Дж при 10 Дж на счету, списывает 10 Дж и 1 кредит, а не «12 Дж в минус». Остаток агента после цикла — `before − spend`, и именно он пишется в отчёт: следующему циклу нужно видеть, чем заплатили.

**Ворота §4.8 безусловны.** В отличие от структурного фильтра §4.5 («нет альтернативы» — тогда разрушающие варианты остаются между собой), нехватка после полной торговли изымает опцию всегда: это вердикт, а не предпочтение. Порядок ворот нормативен — §5 → §4.5 → §4.8: причина, которую читателю нужно узнать первой, — про мир, а не про кошелёк.

**Отчёт расширен, а не переписан.** В §6.2 добавлены `resources_before` / `resources_after`; в строку опции §6.3 — `resource_consumption`, `conversion_applied`, `resources_uncovered`; в строку сущности — `blocks` и `derivation` (процедура и её сырые входы, чтобы блочные числа можно было пересчитать). `removed_options` получил третьи ворота — `insolvency`.

**Реализованная фикстура.** Шесть сущностей: пять прежних плюс `drone`, который объявляет не блоки, а **требование** `energy: 4.0`. Группа одна (`credit` + `energy`), средства агента `credit: 6`, `energy: 10` ⇒ `(c_g, C_g) = (4, 16)`, `x_g = 0.25`, `f_g ≈ 0.7071`. Курс `credit->energy = 2.0` (кредит даёт два джоуля), обмен — 1000 мкс. Проверки строятся на нём: дефицит 2 Дж покупается за 1 кредит; обмен, не укладывающийся в `τ`, оставляет дефицит; ресурс `fuel` не объявлен — остаток неизвестен, значит вход недопустим; производство в одной сущности вычитается из расхода другой (сеть решает). Пример §8.8 с `x_g = 0.6` (`−0.832` ната) остаётся иллюстрацией нормы; реализованная фикстура берёт `x = 0.25`, чтобы требование, обмен и остаток были видны в одном числе.

**Дайджест сменился: `e6f58a7e…` → `bed37c25…`.** Линейка теперь включает ресурсы с единицами и масштабами, группы, курсы, мандат и требования `drone`. Это ровно то, что записано в §10 как цена `v0.6`: две реализации, объявившие один ресурс в разных единицах, — **разные линейки**, и одинаковый дайджест у них был бы ложью.

**Проверено исполнением во всех четырёх портах.** Python 68 проверок, Go 62, C++ 62, Rust 62 (Rust — на `-C opt-level=0` и `2`); все зелёные, ноль `FAIL`, и **один дайджест на всех**: `bed37c25fd9cb757e9ea4a861c01cd4660fd896a83cd39b7c73b8e0be7489ad4`. Прежний `e6f58a7e…` перестал существовать, потому что линейка теперь несёт единицы, группы, курсы и мандат. Проверки строятся на общей фикстуре: производный блок `(4, 16)` у `drone` и те же пер-сущностные числа, что в `v0.5`, у остальных пяти; прямую оплату, покупку дефицита за 1 кредит (обмен 1000 мкс в тот же `τ`, суммарная длительность 2000 мкс), обмен, не уложившийся в `τ`, необъявленный ресурс `fuel` и неоплатимую цену — все три последних заканчиваются `gate = "insolvency"`; и поля отчёта `resources_before`/`resources_after` (покупка списывает ровно тот ресурс, которым заплатили: `{credit: 5.0, energy: 0.0}`), пер-опционный расход, применённую конверсию и непокрытый остаток. Отдельно проверено, что тот же ресурс с другим масштабом даёт другой дайджест — иначе слой ресурсов в линейке был бы декорацией. Строка доказательства дописана в `DOF-SPEC.md` §10.

**Ремонт текста §4.4 (посылка устарела к `v0.6`).** Клауза «Known limitation (reserved)» объясняла, почему жёсткость не выражена в единицах §4.6: «it requires the budget vector in the state and `projected_budget_delta`, **neither of which is normative yet**». После `v0.6` это **ложно**: и средства действующего агента в состоянии, и объявленный расход опции нормативны — под именами `resources` (§3.2) и `projected_resource_delta` (§3.3). Правило не меняется (реализации по-прежнему обязаны использовать `0.5`), и порты не затронуты, поэтому это ремонт **текста**, а не версия: в §4.4 посылка заменена на «prerequisites … became normative in `v0.6` … what remains missing is the definition of `x_rig` itself». Границы прежние: `x_rig` остаётся reserved, дом необратимости (`v0.7`) этим не предрешается.

**Ремонт текста §3.4.1/§3.4.2, снятие слэш-записи пар (внесено 2026-09-16).** Три дефекта сидели в одних и тех же двух предложениях, поэтому правка одна. Прецедент для записей такого рода — `v0.4` text repair: правило не меняется, меняется запись.

1. **Слэш в паре счётчиков читался как дробь.** Нормативная формула живёт как `ψ_var = V / (V + V_env)` (§4.6), а та же пара в списках писалась `V`/`V_env` — с тем же символом деления на месте. Прочтение «отношение `V/V_env`» — это ровно та ошибка, против которой стоит первое правило процедурной части: счётчики это входы, линза это доля, а `V/V_env` — ни то ни другое (безразмерное отношение, неограниченное сверху). Toolkit ту же пару всегда писал через запятую. **Норма:** в списках и перечислениях пара пишется через «и»; символ деления остаётся только формулам.
2. **§3.4.1, закрытый hashed-список:** «the per-entity lens counters: `V`/`V_env`, the Options blocks as `(c_g, C_g)` pairs, `F`/`F_env`» → «…`V` and `V_env`, the Options **inputs** — the raw per-resource requirements together with the derived `(c_g, C_g)` pairs that the procedure of §4.6 produces from them — and `F` and `F_env`». Причина не стилистическая: блоки производные, а все четыре порта хешируют ещё и сырые требования (`"requirements":{"energy":"4.000000"}` — это утверждает проверка в Go/C++/Rust-харнессах). «MUST be present» формально делает список минимумом, но §7 требует совпадения дайджеста с эталоном, значит вход, определяющий числа, обязан быть назван — иначе реализация, читающая только §3.4.1, получит другой дайджест.
3. **§3.4.2, уровни:** «ordered lens set, **budgets**, block partition, rates, `τ`, …» → «the ordered lens set, the acting agent's means per resource **with their units**, the derived groups, the observed rates and the declared mandate, `τ`, …». `budgets` — доменное имя до `v0.6`; `mandate` в перечислении не был назван вовсе.
4. **§4.6, «Anchors» — найдено рядом, тот же класс:** «half the requirement (or half the **budget**)» → «half the means». `C_g` с `v0.6` — сумма средств агента по группе, а «бюджет» как термин в стандарте больше не определён (имя снято в §3.2).

Правил это не меняет: ни одно `MUST` не тронуто, поведение портов прежнее, дайджест `bed37c25…` не сдвинулся — проверено прогоном `patterns/tools/verify_ports.sh`, а не предположением. Запись — в журнале `DOF-SPEC.md` §10.

### 11.7 Необратимость в `v0.7`: закрытие переходов вместо платы

Разбор задачи B по **живому тексту** `DOF-SPEC.md`, а не по памяти этого черновика. Сначала что из остатка B уже закрыто, затем развилки, решения и то, что осталось открытым.

**Что закрыто, а что нет.**

| Пункт остатка B | Состояние | Где |
|---|---|---|
| #1 бюджет-вектор в `State` | **закрыт `v0.6`** | §3.2 `resources` |
| #2 расход у опции | **закрыт `v0.6`** | §3.3 `projected_resource_delta` |
| #3 именованная процедура структуры обмена (группы и курсы как *наблюдение*) | **открыт** | §4.8 говорит «derived from observed exchange paths», но процедуры нет ни в тексте, ни в портах: курсы в фикстурах — константы |
| необратимость в единицах цены (`x_rig`) | **reserved** | §4.4 «Known limitation»; дом — `v0.7` (§11.5 п.9) |
| B-xi: курс наблюдается, а не объявляется | **дыра, названная самим B** | текст говорит «observed», вход — объявленное число; курс остаётся свободным параметром |
| R8 анти-сатурация | нормы нет | живёт только в §8.8 |
| `mandate` — роль | **дыра** | в hashed-содержимом §3.4.1/§3.4.2 есть, в §4.8 — ни слова; порты его хешируют, и он ни на одно число не влияет |
| B-vii «символ унифицировать» | **закрыт `v0.3`** | §11.1 п.14; пункт §8.8 помечен устаревшим и снят (в конце раздела) |
| B1 против B2 | закрыт | B-i/B-iv: `B2`, `4^(−x)` |

Две находки того же разбора, которых в списке остатка не было:

- **`mandate` — компонент линейки без роли** (см. таблицу). Он объявлен частью hashed-содержимого, §3.4.1 ссылается за его определением на §4.8, а §4.8 этого слова не употребляет ни разу.
- **Ветка необратимости мертва во всех четырёх портах.** `is_reversible = false` не ставится **нигде** — ни в одной фикстуре, ни в одном сценарии; ветки `net -= 0.5` (python) и эквивалент в rust написаны и не исполняются ни разу. Значит нынешний зелёный `patterns/tools/verify_ports.sh` про §4.4 не говорит ничего, и любая правка §4.4 сегодня прошла бы «не сдвинув тесты» — это было бы отсутствием доказательства, а не аргументом в пользу правки. Отсюда требование к релизу: **фикстура обязана получить необратимые опции** (ниже, «Следствия»).

**Решение: один артефакт.** Необратимость не решается отдельно от достижимости, потому что у них общий объект — путь в графе мира `G`. Релиз `v0.7` вводит **граф `G` с вердиктами достижимости и именованными процедурами над ним** и тем самым закрывает сразу четыре долга: исключение узла из `calc` (восстановимость), свидетель ярлыка `is_collapse_source`, необратимость как закрытие переходов и валидность курса (B-xi). Раздельные реализации отклонены: пять определений одного объекта дали бы пять разных правителей для одного и того же — то, против чего стоит §7.

**Развилка «`V` или `ψ_opt`» — закрыта на `V`.** Закрытие видно в Variety-линзе: §4.6 уже нормативно говорит, что аффордансы — достижимые сущностью средства — «add response vectors to `V`», поэтому закрытая дверь есть просто минус вектор, и никакого нового символа не нужно. Путь через `ψ_opt` (закрытие **переходов** как сужение набора переходов) записан как **сознательно отложенный**: он требует нового фактора в линзе, а по §4.6 добавление фактора меняет шкалу всех значений в индексе — это отдельный версионный релиз, а не бонус к этому. Артефакт для обеих дверей один и тот же (граф `G`), различается только линза-потребитель, поэтому выбор `V` путь через `ψ_opt` не теряет, а откладывает.

**Клауза: закрытие не изымает саму опцию.** Необратимость — **цена, а не ворота**. Изъятий `v0.7` не прибавляет ни одного, и это проверяемое свойство: если после правки какая-то необратимая опция исчезла из набора, а раньше в нём была, — это дефект правки, а не её смысл.

| Что произошло | Механизм | Судьба опции |
|---|---|---|
| Закрыта **часть** достижимых средств сущности | **цена**: уменьшение доли Variety-линзы, выведенное из `V` | **остаётся в наборе и может победить** |
| Закрыты **все** средства — сущность пришла в известный ноль | плата за коллапс, §4.2 | изымается **только если есть безвредная альтернатива** (§4.5); при одних разрушительных вариантах остаётся и сравнивается между собой |
| Требуемых ресурсов нет после полной торговли | ворота неплатёжеспособности, §4.8 | изымается (вердикт) |

Третий уровень к необратимости отношения не имеет вовсе: необратимость не ресурс, из бюджета не платится, в `resources` и `projected_resource_delta` не входит. Два независимых журнала: **ресурсы отвечают на «состоится ли», необратимость — на «что останется после»**.

**Цена сейчас и вердикт потом — нужны обе половины.** Проекция §4.3 одношаговая: сравниваются `S` и `S′`, поэтому закрытые переходы укусили бы только в следующем цикле. Если оставить лишь правку графа, в момент решения необратимость **не видна вообще** — необратимая опция с тем же `projected_dof_delta` выглядит ровно как обратимая, и правило превращается в ничто. Отсюда: **цена** (сейчас) — чтобы выбор увидел потерю; **вердикт** (в следующем цикле) — чтобы потеря была настоящей, и опции, которым нужны закрытые переходы, изымались вердиктом, а не штрафовались. Цена без вердикта — бухгалтерская фикция; вердикт без цены — слепое сравнение.

**Числа.** Цена пропорциональна тому, сколько свободы реально уничтожено. Сущность с `V = 9`, `V_env = 1` (то есть `ψ_var = 0.9`):

| Закрыто | `V` после | `ψ_var` | Цена |
|---|---|---|---|
| 1 из 10 | 8 | 0.889 | **−0.012** ната |
| 5 из 10 | 4 | 0.800 | −0.118 ната |
| 8 из 10 | 1 | 0.500 | **−0.588** ната |
| все 10 | 0 | 0 | коллапс, `ε`-пол (§4.2) |

Для сравнения: нынешние плоские `0.5` ната эквивалентны разрушению **~80 % средств сущности на любую необратимость**, включая замену лампочки. Новая цена мягка к мелкой необратимости и тяжелеет ровно там, где тяжело; действие, спасающее счётный узел (+4 ната), спокойно побеждает собственное закрытие одной двери (−0.012).

**Два сторожа, без которых цена обходится.**

1. **Собственный путь опции не может быть в её же списке закрытий.** Иначе действие само себя делает неисполнимым — это противоречие, а не «дорого».
2. **Пустой список закрытий при `is_reversible = false` — противоречие.** Это закрывает адверсариальную дыру: сегодня флаг ставит сама опция (генератор ставит `true` — штрафа нет), и ничто его не проверяет. После правки опция обязана **назвать, что закрыла**, а граф это подтверждает — тот же свидетельный порядок, что §8.9.3 требует для ярлыка агрессора. Отсюда: граф обязан быть нормативным, иначе список закрытий нечем проверить и цена снова станет самодекларацией агента.

**Развилка «настраиваемая жёсткость для конкретного агента» — рассмотрена и отклонена в форме веса.** Предложение: сделать цену необратимости настраиваемой per-agent, потому что для одних целей необратимость допустима (удаление файла из `/tmp`), а для других нет (медицинский робот).

Разбирается не весом, а измерением. Удаление файла из `/tmp` закрывает **ничего** — файл не является достижимым средством ни одной счётной сущности в объявленном горизонте, значит `ΔV = 0` и цена ноль **по измерению, а не по скидке**. Медицинский робот упирается в другое: ткань не восстановима в пределах горизонта восстановления, а §8.9.1 уже определяет `T_rec(X)` как величину «based on the entity's type (e.g., biological window for a human)». То есть разница между доменами вводится **на уровне сущности, а не агента**, и это правильнее: дорого не потому, что агент мнительный, а потому что носитель будущих состояний не вернуть.

Отклонённый вариант — **свободный множитель `λ_rig`** в декларации — отвергнут по четырём причинам:

1. Он ломает сравнение агентов: сегодня правитель один на всех, и низкий индекс означает «теряет больше свободы». Агент с `λ_rig = 0` всегда выглядит «свободнее» ровно потому, что закрыл себе глаза — накрутка не решения, а **оценки**, то есть уровень выше и вреднее дыры с курсом, которую закрывает B-xi.
2. Он открывает прецедент: если настраивается жёсткость, следующий вопрос — почему не `λ_var` и `λ_con`, а за ними — почему не наборы аксиом. Стандарт превращается во фреймворк.
3. Он даёт адверсарию ручку «моя необратимость не считается» — та же категория, против которой стоят свидетель ярлыка и запрет генератору подгонять себя под ворота.
4. Он всё равно не отменяет учёт: реальная потеря `V` наступит в следующем состоянии, потому что средства действительно закрыты. Вес управляет не правдой, а близорукостью, выдавая себя за правду.

Отдельно фиксируется: **нынешние плоские `−0.5` — это уже настройка** — скрытая, недекларируемая и одна на всех. Вопрос не в том, вводить ли ручку, а в том, оставлять ли скрытую глобальную константу или заменять её выводимой величиной; `v0.7` заменяет.

Дверь оставлена, но не весом. В стандарте уже есть объявляемый per-agent параметр с ценностным смыслом — приор `u₀` (§4.7), и он закрыт тремя вещами: **жёсткой полосой** (`U_MIN`/`U_MAX` — ниже не опуститься), **аудитом** и **заморозкой по R7**. По этому образцу допустимы две формы: полоса `λ_rig ∈ [λ_min, 1]` с **нормативной** нижней границей, либо объявляемый **горизонт** «как далеко вперёд считается закрытие» в единицах `T_rec` и горизонта `V_env`. Отличие принципиальное: полоса — предпочтение (меняет оценку), горизонт — измерение (меняет то, что видно). Обе добавляются **позже и аддитивно** — они умножают уже выводимую величину, поэтому сейчас это не блокирующее решение. Блокирующее — механизм, и он фиксируется.

**MUST NOT.** Плата за коллапс и структурный фильтр (§4.2/§4.5) не настраиваются ни при каком варианте. Иначе Axiom 3 становится конфигом, и стандарт лицензирует ликвидацию — то, ради запрета чего он написан.

**Объём релиза `v0.7` — закрыто: вариант B (один релиз, полный набор).** Пять долгов связаны не удобством, а общим объектом — путём в графе `G`. Зависимости у них при этом разные, и именно это делает развилку настоящей, а не косметической.

| Долг | Что нужно, кроме `G` | Нужен ли другой долг |
|---|---|---|
| Необратимость (закрытие переходов) | список закрытий + вердикт по графу + проекция `V` | нет |
| Курс (B-xi) и структура обмена (#3) | обменные рёбра + именованная процедура наблюдения | нет |
| Восстановимость (снятие `reserved` с §4.2) | `T_rec` по типу сущности + класс средств `M(S)` + вердикт | нет |
| Свидетель `is_collapse_source` | наблюдаемый акт — ребро, понижающее DoF счётной сущности | нет |
| Ступени 2–3 лестницы §4.5 | восстановимость | да |

Долги независимы друг от друга, но все зависят от `G`: два кластера («решающий контур» и «мир-контур») друг в друге не нуждаются вовсе.

**Форма `G` одна на всех пять потребителей** — типизированные рёбра с эффектами: закрытие = удаление рёбер; обмен = рёбра вида «exchange» с ценой из дешёвейшего пути; восстановимость = существование пути, чьё действие поднимает `DoF` в пределах `T_rec`; акт коллапса = ребро, понижающее DoF счётной сущности; счёт `V` = число достижимых рёбер-средств. Отсюда решение: **схема проектируется один раз, при всех потребителях в поле зрения**. Определить `G` под половину потребителей и через релиз обнаружить, что «класс допустимых средств» в неё не выражается, значит перекроить схему и перевыпустить артефакт с декларацией — то же «пять определений одного объекта дают пять правителей».

| Вариант | Последствие |
|---|---|
| **(A)** только необратимость, без графа | **невозможен**: список закрытий нечем проверить, остаётся коэффициент |
| **(B) один релиз, все пять потребителей** | **принят**: дайджест меняется один раз, фикстура переделывается один раз, и релиз приходит в **синхронное состояние между частями** — спец, декларация, отчёт и порты одной версии, без остаточных `reserved` |
| **(C)** два релиза по кластерам | два изменения дайджеста и две переделки фикстуры подряд; риск перекройки `G`, спроектированного под половину потребителей |
| **(D)** `G` целиком, вердикты считаются, но не влияют до второго релиза | **отклонён**: воспроизводит болезнь `mandate` — компонент в линейке, который все считают, а он ни на что не влияет |

**Внутренний порядок работ** (один релиз, шаги не смешиваются):

1. схема `G` и словарь вердиктов — типизированные рёбра с эффектами, длительностями и расходом ресурсов; `T_rec` по типу сущности;
2. именованные процедуры над `G` — счёт `V`, структура обмена (группы и курсы как наблюдение), вердикты достижимости;
3. закрытие переходов — список у опции, цена через `ψ_var`, два сторожа, `is_reversible` становится производным;
4. свидетель `is_collapse_source` — машинно-проверяемый акт вместо флага;
5. восстановимость — `T_rec`, `M(S)`, исключение из `calc`, снятие `reserved` с §4.2;
6. ступени 2–3 лестницы §4.5 активируются;
7. `mandate` получает роль, R8 становится проверкой §7.

Шаги 3–6 потребляют 1–2, и ни один не требует вердиктов, которых ещё нет.

**Что входит.**

| Что | Куда |
|---|---|
| Схема графа `G`, словарь вердиктов, `T_rec` по типу сущности | новые разделы + §3.4 |
| Именованные процедуры: счёт `V`, структура обмена, вердикты | §3.4.1, §4.6, §4.8 |
| Закрытие переходов: список, цена через `V`, сторожа, `is_reversible` → производное | §4.3, §4.4, §3.3 |
| Свидетель `is_collapse_source` | §4.2 |
| Восстановимость: `M(S)`, `T_rec`, исключение из `calc`, снятие `reserved` | §4.2, §4.7 |
| Ступени 2–3 лестницы | §4.5 |
| `mandate` — роль; R8 — проверка §7 | §4.8, §7 |
| Отчёт: строка закрытий, вердикты, свидетель | §6.2, §6.3 |
| Порты: граф в четырёх языках, новые фикстуры, новый дайджест, запись в §10 | `patterns/*` |

Фикстура релиза обязана нести: две необратимые опции (одна побеждает, одна проигрывает), путь обмена для курса, сущность на известном нуле с путём восстановления, поддельный ярлык агрессора.

**Что не входит.** T1 (полная не-компенсация) — не про артефакт, а про порядок выбора; дом `v0.8`. Опции-измерения — порты их не моделируют, для графа не нужны. Настраиваемая жёсткость (полоса или горизонт по образцу `u₀`) — аддитивно, позже. Axioms/Definitions `SKILL.md` — не трогаем.

**Цена шага.** Дайджест меняется **один раз**: в него входят закрытия, версия процедур наблюдения и вердикты — всё, что определяет числа. Все четыре порта перепрогоняются на новой фикстуре, прежние доказательства становятся историей, в §10 идёт запись версии с новой строкой доказательства. Это версионное событие, а не ремонт текста.

**Схема `G` и словарь вердиктов — закрыто: пакет (c)–(b)–(b).** Три решения сцеплены: где живёт граф определяет, что попадает в дайджест; кто выносит вердикт определяет, можно ли вердикту верить; сколько значений у вердикта определяет, что происходит с неизвестностью.

1. **`G` — артефакт наблюдения: нормируются схема и процедура, а не конкретный граф.** Конкретный граф идёт в отчёт как происхождение; в дайджест — выведенные величины (счёт средств, курсы, закрытия, вердикты). Основание: §3.4.1 уже делит hashed-содержимое (то, что определяет числа) и report context (объяснение), и `v0.6` уже работает по этому порядку — `derive_blocks`: «derived numbers MUST equal what that procedure computes from those inputs». Кажущееся противоречие «граф обязан быть нормативным» снимается: нормативны схема и процедура, конкретный граф — наблюдение, проверяемое пересчётом. Отклонено: граф в состоянии (смешивает наблюдение с проекцией и ломает заморозку R7 — проекция опции начинает переписывать мир) и граф целиком в дайджесте (большой объект в линейке и самый рискованный шаг канонизации в четырёх языках).
2. **Perception поставляет граф, ядро считает вердикты детерминированно.** Вердикт не может быть ни набором опций генератора (§4.2 это уже запрещает), ни утверждением наблюдателя. Разделение труда то же, что в `v0.6` (среда даёт предложение, агент даёт средства): здесь Perception даёт граф, ядро даёт вердикт. Ярлык `is_collapse_source` становится производным — подтверждается наблюдаемым актом в графе, а не выставляется флагом.
3. **Вердикт трёхзначен: `reachable` / `proven_unreachable` / `undetermined`, fail-safe в сторону видимости.** Исключение из `calc` разрешает только `proven_unreachable` со свидетелем; `undetermined` оставляет сущность в индексе и оценивается `u(t)` (§4.7). Прецедент нормативен: §4.2 уже требует, чтобы сущность с `dof_known == false` никогда не исключалась.

Следствия, вытекающие механически:

- **Неполнота наблюдения не может читаться как доказательство.** Отсутствие ребра — факт наблюдения; неполное наблюдение даёт `undetermined`, а не `proven_unreachable`. Это норма, а не совет.
- **Типология `T_rec` решается сама:** объявленный горизонт по типу сущности; не объявлен или не определён ⇒ `undetermined` ⇒ исключения нет. Отдельной развилки не остаётся.
- **Форк «курс — объявленный вход или наблюдение» схлопывается в этот пакет.** Решением 2 курс становится выходом именованной процедуры над `G`; решением 1 в дайджест идёт выведенная величина курса, а его происхождение (путь, из которого он выведен) — в report context. Самостоятельной развилки не остаётся, остаётся определение процедуры (шаг 2 порядка работ).

**Проверки на ловушки.** Каждая — с условием падения, чтобы ловушку можно было поймать, а не только назвать.

| Ловушка | Проверка | Где живёт | Чем падает |
|---|---|---|---|
| **Генераторозависимость** | вердикты и `calc(S)` не меняются при смене набора опций на том же графе; прогон с **пустым** набором кандидатов даёт **те же** вердикты | §7 (пункт конформности) + фикстура во всех четырёх портах | два прогона, разные наборы, разные вердикты |
| **Утверждение вместо свидетеля** | `is_collapse_source = true` без наблюдаемого акта в графе — **неконформно**: ярлык не принимается, а выводится | §4.2 + фикстура (поддельный ярлык) | сущность с флагом и без акта — должна остаться в `calc` |
| **Подмена «недостижимо» дороговизной** | `proven_unreachable` влечёт отсутствие пути в графе (структурно), а цена всякого присутствующего ребра конечна; достижимый путь с астрономической ценой остаётся `reachable`, и изымает его ворота §4.8, а не вердикт | §4.2/§4.8 + фикстура | два исхода обязаны быть различимы в отчёте; отдельный тест «цена присутствующего ребра конечна» |
| **Проза в дайджесте** | `M(S)` входит в hashed-содержимое как **канонически сериализованная структура** (идентификаторы категорий допустимых средств), а не свободным текстом. Мутационная проверка в обе стороны: правка пояснения/происхождения дайджест **не** меняет; правка идентификатора категории или горизонта — **меняет** (категория решает допустимость, значит определяет числа) | §3.4.1/§3.4.3 + фикстура | две мутации, два противоположных ожидания |
| **`T_rec` как средство сужения** | монотонность: `T_rec′ ≤ T_rec ⇒ excluded(T_rec′) ⊆ excluded(T_rec)` — сокращение горизонта не расширяет множество исключённых; необъявленный горизонт ⇒ `undetermined` ⇒ сущность остаётся | §4.2/§4.7 + фикстура | два горизонта, проверка вложенности множеств; тест отсутствующего горизонта |
| **Размерностный дефект в новом символе** | в правиле закрытия нет ни одной константы: цена пересчитываема из `V_before`/`V_after`; `x_rig` не определяется вовсе | §4.4 + §7 | цена меняется при мутации счётчиков; текстовый поиск на отсутствие константы |
| **Обход цены пустым списком** | `is_reversible = false` при пустом списке закрытий — противоречие; собственный путь опции не может быть в её списке закрытий | §4.4/§3.3 | обе конфигурации обязаны отвергаться |

Проверки 1–3 — решающего контура, 4–7 — контура закрытия. Все семь обязаны быть в фикстуре релиза прежде, чем `v0.7` будет объявлена готовой.

**Открыто, ждёт решения (не блокирует запись §11.7).**

- **`mandate`.** Рекомендация: вернуть роль из B-ix (`C_g = min` из доступных средств, внешнего лимита и мандата) — он уже в линейке и уже во всех четырёх портах, убирать дороже, чем доопределить. Симметричная страховка: средства объявляет сам агент, поэтому §7 должен требовать от отчёта **происхождение** средств (замеренный баланс против заявленного полномочия) — «неизвестен свой остаток — недопустимый вход» закрывает незнание, но не завышение.
- **R8.** Рекомендация: не вводить константой, дописать в §7 как проверку конформности — объявленные средства обязаны быть средствами (draft-формула `ψ ≥ 4^(−k)` как готовый критерий).
- **Ступень лестницы «при равных — предпочитай обратимое» не добавляется.** Цена уже различает; лексикографическое предпочтение даже при меньшем выигрыше — это не-компенсация, то есть T1 и `v0.8`. Два разных механизма в одном релизе не смешиваются.

**Следствия (заготовка, правки — отдельным шагом после фиксации развилок).**

| Куда | Что |
|---|---|
| §4.4 | плоские `−0.5` уходят **версионным** изменением, не ремонтом текста: числа всех портов сдвинутся, дайджест сменится. `x_rig` не определяется вовсе — это было имя заглушки |
| §4.3 | проекция перестаёт быть «дельта к `current_dof` только»: она обязана уметь менять **счётчик** `V` — иначе закрытие не видно вовсе |
| §3.3 | у опции появляется список закрытий (правка графа); `is_reversible` становится **производным** полем отчёта: `список пуст ⇔ обратимо` |
| §3.2, §3.4.1 | `V` попадает в проектируемое; свидетель закрытия — из графа |
| §6.3 | строка закрытий и её цена построчно — рядом с платой за коллапс |
| §7 | R8 как проверка; происхождение средств у `mandate` |
| фикстура | **две** необратимые опции: одна должна **победить** (необратима, но спасает счётный узел), одна должна **проиграть** (необратима и ничего не даёт). Без них ветка §4.4 остаётся непроверенной |

**Ремонт черновика, попутно.** Пункт 1 дефектов в конце §8.8 («§4.3 определяет `NetDelta_index`, а §4.4 вычитает из `NetDelta`») **устарел**: расхождение символов закрыто в `v0.3` (§11.1 п.14), и `NetDelta_index` в `DOF-SPEC.md` не встречается ни разу. Пометка снята, пункт 2 (§4.4) переадресован сюда.
